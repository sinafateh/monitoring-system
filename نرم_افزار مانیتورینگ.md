 

 

 

 

 

 

 

 

 

**نرم‌افزار مانیتورینگ**

 **اعلام حریق**

 

 

 

 

 

 

 

 

 

 

 

**فهرست**  
1\.  معرفی کلی نرم‌افزار 9

**1.1 نکات فنی برای استفاده در بستر وب و موبایل**   **9**

2\. راه‌اندازی نرم‌افزار (Boot Screen) 10

3. ثبت‌نام و ورود کاربران (مدیریت کاربران) 10

3.1 **ثبت‌نام کاربر** (Sign Up) **10**

فرآیند ثبت‌نام پس از تأیید اطلاعات: 10

3.2 **ورود به نرم‌افزار** (Login) **11**

3.3 **فراموشی رمز عبور**  **11**

3.3.1 اطلاعات مورد نیاز برای بازیابی رمز: 11

3.3.2 دکمه‌ها: 11

3.3.3 فرآیند بازیابی رمز: 11

3.3.4 صفحه تعیین رمز جدید: 11

4. مدیریت پروژه‌ها 12

4.1 **صفحه اصلی پس از ورود مدیر**  **12**

**4.2. ایجاد پروژه جدید** (New Project) **12**

4.2.1 اطلاعات پروژه: 12

**4.3. باز کردن پروژه** (Open Project) **12**

4.3.1 اطلاعاتی که کاربر وارد می‌کند: 12

4.3.2 اطلاعاتی که نمایش داده می‌شود: 13

5\. منوی اصلی نرم‌افزار 14

**5.1 منوی اصلی** (Main Menu) **14**

6\. منوی تنظیمات (Secondary / Mega Menu) 15

**6.1.** System **(تنظیمات سیستم)**  **16**

6.1.1 Setting  (تنظیمات عمومی) 16

Relay Output 6.1.2 (خروجی رله‌ها) 17

6.1.3 Advanced (پیشرفته) 18

6.1.3.1 Active Loop Card (فعال کردن لوپ کارت) 18

6.1.3.2 Network (شبکه) 19

6.1.4 Night Mode (حالت شب و روز) 20

**6.2.** Loop card **(کارت لوپ)**  **21**

**6.3.** Group **(گروهبندی)**  **24**

6.3.1 Zone Group  (گروهبندی زون) 24

6.3.2 Input & Output Group  (گروهبندی ورودی و خروجی) 25

6.3.2.1 انتخاب شماره گروه‌ها 25

6.3.2.2 Input / Output Grouping. 26

6.3.2.2.1. Loop Number (شماره لوپ): 26

6.3.2.2.2.Active Count (تعداد فعال) : 26

6.3.2.2.3.Output Active for (فعال‌سازی خروجی برای): 26

6.3.2.2.4. Delay (0–999 Sec) (تاخیر): 27

6.3.2.2.5. Status(وضعیت): 27

6.3.2.2.6. Pre View (پیش نمایش): 27

6.3.2.3 دکمه های مدیریتی.. 27

6.3.2.3.1. Read All Groups (خواندن همه گروه‌بندی‌ها): 27

6.3.2.3.2. Save (ذخیره): 27

6.3.2.3.3. Delete (حذف): 27

6.3.2.3.4. Delete All (حذف همه): 27

6.3.2.3.5. Update All (به روزرسانی همه): 27

6.3.2.3.6. Print (چاپ): 27

**6.4.** Features **(ویژگی ها):** **29**

6.4.1. تنظیمات ویژگی‌ها 29

6.4.2. دکمه‌های موجود در تب Features. 29

**6.5.** Events **(رویدادها):** **30**

6.5.1 امکانات و گزینه‌ها 30

6.5.2 نمایش رویدادها بر روی نقشه (2D / 3D) 30

6.5.3 جزئیات تعاملی رویدادها روی نقشه. 30

6.5.4 ثبت و نمایش اقدامات کاربران. 31

**6.6**Remote Panel  **(ریموت پنل)**  **32**

6.6.1 انتخاب و اتصال پنل‌ها 32

6.6.2 نمایش وضعیت پنل ریموت.. 32

6.6.3 مدیریت هم‌زمان چند پنل.. 32

6.6.4 همگام‌سازی اطلاعات (Synchronization) 32

6.6.5 ثبت و گزارش‌گیری ارتباطات.. 33

6.6.6 ملاحظات عملکردی و امنیتی.. 33

**6.7**Report **(گزارش)**  **34**

6.7.1 Devices Not Configured in any Group. 34

6.7.2 Devices Not Configured in any Group. 34

6.7.3 Configured Groups. 34

6.7.4 چاپ و خروجی گزارش‌ها 34

**6.8** GSM  **(تلفن‌کننده)**  **35**

6.8.1 مدیریت پروژه (Project Management) 35

6.8.3 مدیریت رویدادها (Event Management) 35

6.8.3.1 افزودن رویداد (Add Event) 35

6.8.3.2 نوع رویدادهای قابل ارسال. 36

6.8.4 اتصال و ارتباط (Communication) 36

6.8.4.1 اتصال بلوتوث (Bluetooth Connection) 36

6.8.4.2 اتصال از طریق اینترنت (Internet Connection) 36

6.8.4.3 همگام‌سازی تنظیمات (Synchronization) 37

6.8.4.4 ملاحظات امنیتی ارتباط.. 37

**6.9**Monitoring  **(مانیتورینگ)**  **38**

6.9.1. منوی اختصاصی نرم‌افزار مانیتورینگ... 38

6.9.2. ساختار تب‌ها در بخش مانیتورینگ... 39

6.9.1.1 مدیریت ارتباطات.. 40

6.9.1.1.1. تنظیم پروتکل ارتباطی نرم‌افزار با پنل.. 40

6.9.1.1.2. وارد کردن آدرس پروژه 40

6.9.1.1.3. تنظیم ارتباط سریال (Serial Port) 40

6.9.1.1.4. تنظیم ارتباط شبکه (Network Port) 41

6.9.1.1.5. اعمال تنظیمات.. 41

6.9.1.2 مانیتورینگ وضعیت اتصال (Connection Monitoring) 42

6.9.1.2.1. نمایش وضعیت اتصال (Connection Status) 42

6.9.1.2.2. نحوه نمایش بصری (UI Design) 43

6.9.1.2.3. نمایش جزئیات تکمیلی (Tooltip / Hover) 43

6.9.1.2.4. رفتار سیستم در زمان قطع ارتباط.. 43

6.9.1.2.5. به‌روزرسانی وضعیت.. 43

6.9.1.3 همگام‌سازی اطلاعات (Data Synchronization) 44

6.9.1.3.1. منطق کلی عملکرد (Overall Functional Logic) 44

6.9.1.3.1.1. حالت اول: عدم وجود اختلاف اطلاعات (No Data Conflict) 44

6.9.1.3.1.2. حالت دوم: وجود اختلاف اطلاعات (Data Conflict Detected) 45

6.9.1.3.2. محل قرارگیری در رابط کاربری.. 46

6.9.1.3.3. انواع همگام‌سازی.. 46

همگام‌سازی خودکار (Automatic Sync) 46

همگام‌سازی دستی (Manual Sync) 46

6.9.1.3.4. داده‌های قابل همگام‌سازی.. 47

6.9.1.3.5. ثبت گزارش همگام‌سازی (Synchronization Log) 47

6.9.1.2. نمایش گرافیکی سیستم  (Visualization) 48

6.9.1.2.1. بارگذاری نقشه یا ورود اولیه به بخش نمایش گرافیکی.. 48

فرمت‌های قابل پشتیبانی نقشه. 48

ثبت عنوان نقشه. 48

دکمه تأیید عنوان: 49

دکمه حذف نقشه. 49

پاپ‌آپ تأیید حذف نقشه. 49

دسترسی به تنظیمات نقشه و جانمایی تجهیزات.. 49

افزودن نقشه‌های بیشتر. 50

تعداد نقشه‌ها 50

نکات طراحی حرفه‌ای (Design Considerations \-پیشنهادی) 50

6.9.1.2.2. جانمایی تجهیزات (Device Placement) 51

ورود به صفحه جانمایی تجهیزات.. 51

ساختار صفحه جانمایی تجهیزات.. 51

منوی تنظیمات سمت راست با قابلیت پین  (Pin) 52

نحوه جانمایی تجهیزات روی نقشه. 53

افزودن دیوایس به نقشه. 55

**افزودن دیوایس به نقشه** (Device Placement on Map) **55**

**۱**. **افزودن دیوایس از طریق نمایش درختی** (Tree View) **55**

ظاهر دیوایس‌ها در حالت درختی.. 55

نحوه افزودن دیوایس به نقشه (Drag & Drop) 55

رفتار دیوایس پس از جانمایی.. 55

قابلیت قفل‌کردن دیوایس روی نقشه. 55

حذف دیوایس از نقشه. 56

**۲**. **افزودن دیوایس از طریق نمایش لیستی** (List View) **56**

ظاهر دیوایس‌ها در حالت لیستی.. 56

نحوه افزودن دیوایس به نقشه. 56

رفتار دیوایس پس از جانمایی.. 56

قفل‌کردن و حذف دیوایس... 56

**۳**. **افزودن دیوایس به‌صورت مستقیم از روی نقشه**  **57**

انتخاب ناحیه روی نقشه. 57

انتخاب شماره دیوایس... 57

جانمایی نهایی.. 57

**تعامل با دیوایس‌های جانمایی‌شده روی نقشه**  **57**

**ذخیره‌سازی دیوایس‌ها** **58**

6.9.3**.** **مدیریت رویدادها** (Event Management) **59**

6.9.3.1. دریافت و نمایش آنلاین رویدادها 59

6.9.3.2. واکنش نرم‌افزار به رویدادها (مدیریت آلارم‌ها) 59

6.9.3.2.1. آلارم حریق (Fire Alarm) 59

امکانات نقشه در نمایش آلارم حریق.. 60

مدیریت آلارم حریق در چند نقشه به‌صورت هم‌زمان. 60

رفتار نرم‌افزار هنگام تغییر تب‌ها 60

دکمه‌های کنترلی آلارم حریق.. 61

6.9.3.2.2 آلارم خطا (Fault Alarm) 62

اطلاعات نمایش‌داده‌شده برای آلارم خطا 62

امکانات نقشه در نمایش آلارم خطا 62

مدیریت آلارم خطا در چند نقشه به‌صورت هم‌زمان. 62

دکمه‌های کنترلی آلارم خطا 63

نکات مهم در مدیریت رویدادها و آلارم‌ها 63

۶.۹.4 مدیریت گزارش‌ها  (Report Management) 65

انتخاب بازه‌ی زمانی گزارش... 65

نمایش گرافیکی رویدادها روی نقشه. 65

نمایش اطلاعات با حرکت موس (Tooltip) 65

مدیریت چند نقشه در یک بازه‌ی زمانی.. 66

نحوه نمایش نقشه‌ها به‌صورت تب (Tab) 66

رفتار پیش‌فرض سیستم. 66

قابلیت‌های جابجایی و بزرگ‌نمایی نقشه. 66

جستجو و فیلتر رویدادها 67

جداول اطلاعاتی زیر هر نقشه. 67

جدول اول: جزئیات رویدادهای سیستم. 67

جدول دوم: گزارش فعالیت کاربران سیستم. 67

مدیریت حجم اطلاعات در جداول. 68

چاپ و خروجی گزارش‌ها 68

چاپ کلی گزارش‌ها 68

اطلاعات درج‌شده در سربرگ گزارش چاپی.. 68

6.9.5 کنترل و فرمان‌دهی از راه دور (Remote Control & Command) 69

۶.۹.۵.۱ قطع صدای هشدار پنل (بازر) 69

نمایش وضعیت فعال بودن بازر پنل.. 69

روش‌های دسترسی به بخش قطع صدای بازر 69

نمایش نقشه و وضعیت خطاها 69

قابلیت‌های جابجایی و بزرگ‌نمایی نقشه. 70

جابجایی نقشه. 70

اسکرول خودکار 70

ابزار دست (Pan Tool) 70

بزرگ‌نمایی و کوچک‌نمایی (Zoom) 70

نمایش اطلاعات خطا با حرکت موس (Tooltip) 70

جدول اطلاعات خطاها 70

نحوه قطع صدای بازر پنل.. 71

ثبت عملیات در تاریخچه سیستم. 71

6.9.5.2 فعال‌سازی و غیرفعال‌سازی آژیرها 72

1\. نمایش نقشه دوبعدی و سه‌بعدی در صفحه فعال تب.. 72

مدیریت نمایش نقشه‌ها و تب‌بندی.. 72

امکانات کار با نقشه. 72

نمایش و انتخاب دیوایس‌ها روی نقشه. 73

انتخاب و مدیریت آژیرها 73

پاپ‌آپ تأیید برای دکمه‌ها: 73

قابلیت انتخاب آژیرها در چند نقشه. 74

تغییر وضعیت آژیرها 74

پیام هشدار در صورت عدم انتخاب آژیر. 74

2\. انتخاب گروه آژیرها 74

پاپ‌آپ تأیید برای دکمه‌ها: 75

3\. وارد کردن آدرس آژیر. 75

4\. دکمه‌های فعال/غیرفعال‌سازی کل آژیرها 76

**نکات مهم در ثبت عملیات**	**76**

6.9.6 تست و عیب‌یابی تجهیزات.. 77

نحوه نمایش و انتخاب نقشه‌ها 77

مشاهده اطلاعات دیوایس‌ها 77

فعال‌سازی و غیرفعال‌سازی دیوایس‌ها 77

نکات مهم. 77

**ثبت فعالیت کاربران** (Audit Log) **78**

 

 

**نرم افزار پنل اعلام حریق :**

# **1\.  معرفی کلی نرم‌افزار**

این نرم‌افزار یک سامانه مدیریت و مانیتورینگ سیستم اعلام حریق است که به صورت **اپلیکیشن تحت وب** طراحی شده و قابلیت اجرا و استفاده مستقیم داخل مرورگر وب در لپ‌تاپ، دسکتاپ و تلفن همراه را دارد. کاربران می‌توانند بدون نیاز به نصب برنامه جداگانه، تنها با باز کردن مرورگر خود، به نرم‌افزار دسترسی پیدا کنند و:

·         مدیریت تنظیمات سیستم اعلام حریق را به‌صورت نرم‌افزاری و بدون نیاز به تجهیزات جانبی انجام دهند.

·         وضعیت سیستم و تجهیزات متصل را به‌صورت لحظه‌ای و آنلاین مانیتور کنند.

·         نقشه ساختمان را مشاهده کرده و در صورت بروز حریق یا خطا، محل دقیق وقوع رویداد را به‌صورت گرافیکی روی نقشه مشاهده نمایند.

این نرم‌افزار دارای بخش مدیریتی پیشرفته و سطوح دسترسی مختلف برای کاربران است که امنیت و مدیریت آسان را تضمین می‌کند.

در این نرم‌افزار، تمامی فعالیت‌های انجام‌شده توسط کاربران پس از ورود به سیستم، به‌صورت کامل ثبت و ذخیره می‌شود. هر کاربر با **نام کاربری اختصاصی** خود که وارد نرم‌افزار می‌شود، هرگونه عملیات از جمله **ایجاد، ویرایش یا حذف اطلاعات** را انجام دهد، جزئیات آن عملیات در بخش **گزارش‌ها (Report)** سیستم ثبت خواهد شد.

اطلاعات ثبت‌شده برای هر رویداد شامل موارد زیر است:

·         نام کاربری فرد انجام‌دهنده عملیات  
·         نوع عملیات انجام‌شده (ایجاد، ویرایش، حذف)  
·         بخش یا ماژولی که تغییر در آن انجام شده است  
·         شرح دقیق تغییرات اعمال‌شده  
·         تاریخ و زمان دقیق انجام عملیات

این گزارش‌ها به‌منظور **افزایش امنیت، قابلیت پیگیری (Traceability)، مدیریت کاربران و بررسی خطاها** در سیستم ذخیره شده و فقط برای کاربران دارای سطح دسترسی مجاز (مانند مدیر سیستم) قابل مشاهده خواهد بود.

 

## **1.1 نکات فنی برای استفاده در بستر وب و موبایل**

·         نرم‌افزار به صورت کاملاً ریسپانسیو طراحی شده تا در دستگاه‌های مختلف (دسکتاپ، لپ‌تاپ، تبلت و موبایل) بهترین تجربه کاربری را ارائه دهد.

·         کاربران می‌توانند با ایجاد **شرت‌کات (Shortcut)** روی صفحه اصلی موبایل یا دسکتاپ، دسترسی سریع و مستقیم به نرم‌افزار را داشته باشند.

·         آیکن این شرت‌کات، لوگوی اختصاصی شرکت/نرم‌افزار شما خواهد بود که می‌تواند در قالب فایل‌های PNG یا SVG در وب‌سایت قرار گیرد.

 

# **2\. راه‌اندازی نرم‌افزار (Boot Screen)**

·         هنگام اجرای نرم‌افزار در مرورگر، یک صفحه ساده با لوگوی شرکت و نمایش نسخه نرم‌افزار به همراه یک انیمیشن لودینگ (Spinner  یا  Progress Bar) نمایش داده می‌شود تا کاربر از بارگذاری نرم‌افزار مطلع شود.

·         این صفحه تا زمانی نمایش داده می‌شود که نرم‌افزار به طور کامل برای کاربر یا مدیر (هر فردی که وارد شده است) آماده و بارگذاری شود و سپس به صورت خودکار به محیط اصلی نرم‌افزار منتقل می‌شود.

·         طراحی صفحه به صورت مینیمال و سبک بوده تا سریع بارگذاری شود و کاربر سریع‌تر به محیط اصلی نرم‌افزار دسترسی پیدا کند.

# **3\. ثبت‌نام و ورود کاربران (مدیریت کاربران)**

پس از اتمام صفحه لودینگ (Loading Screen)، صفحه ثبت‌نام یا ورود به نرم‌افزار برای کاربران نمایش داده می‌شود تا بتوانند به حساب کاربری خود دسترسی پیدا کنند.

## **3.1 ثبت‌نام کاربر (Sign Up)**

در صورتی که کاربر قبلاً ثبت‌نام نکرده باشد، باید اطلاعات زیر را وارد کند:

1\.    شماره موبایل

2\.    نام و نام خانوادگی

3\.    نام کاربری

4\.    رمز عبور

5\.    نوع کاربر:

o        مدیر (Admin)

o        کاربر (User)

**نکات مهم:**

·         راه‌اندازی اولیه پنل فقط توسط **کاربر مدیر** امکان‌پذیر است.

·         حداکثر **5 کاربر** می‌توان برای هر سیستم اعلام حریق تعریف کرد.

### **فرآیند ثبت‌نام پس از تأیید اطلاعات:**

**در سمت سیستم:**

·       نرم‌افزار به‌صورت خودکار **Serial Number  مادربرد اصلی پنل** را از اطلاعات سخت‌افزاری دریافت می‌کند.

·       اطلاعات به سرور مرکزی ارسال می‌شود.

·       در صورت تطابق اطلاعات ثبت‌نام با سریال پنل، یک **کد تأیید** به شماره موبایل کاربر ارسال می‌شود.

**در سمت کاربر:**

·         صفحه تغییر کرده و صفحه **وارد کردن کد تأیید** نمایش داده می‌شود.

·         با وارد کردن کد پیامکی، ثبت‌نام کاربر تکمیل می‌شود.

## **3.2 ورود به نرم‌افزار (Login)**

کاربری که قبلاً ثبت‌نام کرده است می‌تواند از یکی از روش‌های زیر وارد نرم‌افزار شود:

**1\.**      **نام کاربری \+ رمز عبور**

**2\.**      **شماره موبایل ثبت‌شده \+ دریافت کد یک‌بارمصرف (OTP)**

## **3.3 فراموشی رمز عبور**

در صفحه ورود، گزینه‌ای با عنوان **«فراموشی رمز عبور»** وجود دارد.

### **3.3.1 اطلاعات مورد نیاز برای بازیابی رمز:**

1\.      نام و نام خانوادگی

2\.      شماره موبایل

### **3.3.2 دکمه‌ها:**

·         تأیید

·         انصراف

### **3.3.3 فرآیند بازیابی رمز:**

**در سمت سیستم:**  
·         اطلاعات واردشده به سرور ارسال می‌شود.

·         در صورت تطابق، یک کد تأیید برای کاربر پیامک می‌شود.

**در سمت کاربر:**  
·         صفحه ورود کد نمایش داده می‌شود.

·         پس از وارد کردن کد، صفحه تعیین رمز جدید باز می‌شود.

### **3.3.4 صفحه تعیین رمز جدید:**

·         اطلاعات غیرقابل ویرایش:

o        نام و نام خانوادگی

o        نام کاربری

o        شماره موبایل

·         اطلاعات قابل ویرایش:

o        رمز عبور جدید

o        تکرار رمز عبور جدید

پس از تأیید، رمز جدید در پایگاه داده ذخیره می‌شود.

# **4\. مدیریت پروژه‌ها**

## **4.1 صفحه اصلی پس از ورود مدیر**

پس از ورود کاربر مدیر، صفحه‌ای با گزینه‌های زیر نمایش داده می‌شود:

1\.      **New** \- ایجاد پروژه جدید

2\.      **Open** \- باز کردن پروژه‌های قبلی

3\.      **About** \- نمایش اطلاعات دستگاه (مدل، نوع، مشخصات)

4\.      **Exit** \- خروج از نرم‌افزار

## **4.2. ایجاد پروژه جدید (New Project)**

با انتخاب گزینه **New**، فرم ایجاد پروژه جدید نمایش داده می‌شود.

### **4.2.1 اطلاعات پروژه:**

1\.    نام و نام خانوادگی مجری پروژه \*

2\.    نام شرکت اجراکننده / ساختمان \*

3\.    عنوان پروژه \*

4\.    آدرس دقیق پروژه \*

5\.    تصویر پروژه (پلن یا نقشه ساختمان)

6\.    مدل پنل (انتخاب از لیست کشویی) \*

7\.    تعداد دیوایس‌های سیستم \*

o  	امکان ورود دستی تعداد هر دیوایس

o  	دکمه **«خواندن از روی دستگاه»** برای دریافت خودکار اطلاعات

o  	پس از خواندن خودکار، امکان ویرایش دستی وجود دارد

8\.    تاریخ و زمان ایجاد پروژه

o  	به‌صورت خودکار از سیستم دریافت می‌شود

9\.    توضیحات تکمیلی (اختیاری)

## **4.3. باز کردن پروژه (Open Project)**

با انتخاب گزینه **Open**، کاربر می‌تواند پروژه‌های قبلی را باز کند.

### **4.3.1 اطلاعاتی که کاربر وارد می‌کند:**

1\.    مدل پنل

2\.    عنوان پروژه

3\.    نوع اتصال پنل به سیستم:

o        اتصال از طریق کابل USB

o        اتصال از طریق کابل شبکه (Ethernet)

o        اتصال از طریق اینترنت (اتصال از راه دور)

### **4.3.2 اطلاعاتی که نمایش داده می‌شود:**

1\.    نام و نام خانوادگی مجری

2\.    نام شرکت اجراکننده

3\.    آدرس پروژه

4\.    تصویر پروژه

5\.    تاریخ و زمان ایجاد پروژه

6\.    توضیحات

پس از تأیید، کاربر وارد محیط اصلی پروژه می‌شود.

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

# **5\. منوی اصلی نرم‌افزار**

 طراحی منوها پیشنهاد می‌شود مشابه منوهای نرم‌افزار Microsoft Word باشد، به‌گونه‌ای که کاملاً گرافیکی طراحی شده و زیر هر عنوان، تصویری مرتبط با آن موضوع قرار گرفته باشد.

## **5.1 منوی اصلی (Main Menu)**

**1\.**    **File**  
·  	New (جدید)  
·  	Open (بازکردن فایل قدیمی)  
·  	Import (وارد کردن)  
·  	Export (خارج کردن)  
·  	Exit (خروج)  
**2\.**    **Firmware Update**  
**3\.**    **Communication**  
·  	Connect  
·  	Disconnect  
**4\.**    **Exit**

**وضعیت اتصال پنل:**

·         با کلیک روی **Connect**:

o        صفحه به حالت نیمه‌شفاف درمی‌آید

o        یک آیکون چرخان نمایش داده می‌شود (در حال بررسی)

·         نتیجه اتصال در سمت راست منو نمایش داده می‌شود:

o        Panel Status: Connected                             	

o        Panel Status: Disconnected	

 

 

 

 

 

 

 

# **6\. منوی تنظیمات (Secondary / Mega Menu)**

این منو برای تنظیمات و مدیریت پیشرفته سیستم اعلام حریق استفاده می‌شود. و در پایین منوی اصلی قرار دارد.

**گزینه‌ها:**

1\.    سیستم System

·         Setting (تنظیمات)

·         Relay Output (خروجی رله)

·         Advanced (پیشرفته)

o       Active LoopCard (کارت لوپ فعال)

o       Network (شبکه)

·         Night Mode (حالت شب)

2\.    LoopCard (کارت لوپ)

3\.    Group (گروه)

4\.    Features (قابلیت‌ها)

5\.    Events (رویدادها)

6\.    Remote Panel (پنل از راه دور)

7\.    Report (گزارش‌ها)

8\.    GSM (تلفن کننده)

·         Customize (سفارشی سازی)

·         Location (موقعیت)

9\.              Monitoring (مانیتورینگ)

 

 

 

## **6.1. System (تنظیمات سیستم)**  

این تب مخصوص تنظیمات کلی سیستم  یا پنل است.  که دارای زیر منوهایی است.

### **6.1.1 Setting  (تنظیمات عمومی)**

·         Date & Time (تاریخ و زمان): تنظیم و نمایش تاریخ و زمان سیستم. این اطلاعات برای ثبت دقیق رویدادها و گزارش‌ها بسیار مهم است و باید همیشه به‌روز باشد.

·         Language (زبان): انتخاب زبان نمایش نرم‌افزار. کاربران می‌توانند زبان مورد نظر خود را انتخاب کنند تا رابط کاربری به آن زبان نمایش داده شود.

·         Panel Caption (عنوان پنل): تعیین عنوان یا نام دلخواه برای پنل اعلام حریق که در قسمت‌های مختلف نرم‌افزار نمایش داده می‌شود و به شناسایی آسان‌تر پنل کمک می‌کند.

·         Password Change (تغییر پسورد): امکان تغییر رمز عبور حساب کاربری برای افزایش امنیت دسترسی به نرم‌افزار. توصیه می‌شود پسورد را به صورت دوره‌ای تغییر دهید.

**دکمه‌ها:**

·         **Read (خواندن):** با انتخاب این دکمه، نرم‌افزار کلیه اطلاعات و تنظیماتی را که قبلاً به‌صورت دستی از طریق خود پنل اعلام حریق وارد شده‌اند، مستقیماً از پنل خوانده و در محیط نرم‌افزار نمایش می‌دهد. این گزینه برای مشاهده و همگام‌سازی تنظیمات فعلی پنل با نرم‌افزار استفاده می‌شود.

·         **Save (ذخیره):** با انتخاب این دکمه، تمامی تغییرات و تنظیماتی که کاربر در نرم‌افزار اعمال کرده است، در پنل اعلام حریق ذخیره شده و جایگزین تنظیمات قبلی می‌شود.

**نکته :** پس از هر عملیات، پیام پاپ‌آپ موفقیت یا خطا نمایش داده می‌شود.

### **Relay Output 6.1.2 (خروجی رله‌ها)**

برای هر رله امکان انتخاب عملکرد وجود دارد:

·         Relay 1 / Relay 2 / Relay 3

o       Fire

o       Fault

o       Supervisory

o       Pre-Alarm

با استفاده از چک باکس یا لیست بازشو می توان تعیین کرد که هر رله برای کدام مورد استفاده شود.

**دکمه‌ها:**

·         **Read (خواندن):** با انتخاب این دکمه، نرم‌افزار کلیه اطلاعات و تنظیماتی را که قبلاً به‌صورت دستی از طریق خود پنل اعلام حریق وارد شده‌اند، مستقیماً از پنل خوانده و در محیط نرم‌افزار نمایش می‌دهد. این گزینه برای مشاهده و همگام‌سازی تنظیمات فعلی پنل با نرم‌افزار استفاده می‌شود.

·         **Save (ذخیره):** با انتخاب این دکمه، تمامی تغییرات و تنظیماتی که کاربر در نرم‌افزار اعمال کرده است، در پنل اعلام حریق ذخیره شده و جایگزین تنظیمات قبلی می‌شود.

 

**نکته :** پس از هر عملیات، پیام پاپ‌آپ موفقیت یا خطا نمایش داده می‌شود.

 

 

###  **6.1.3 Advanced (پیشرفته)**

#### **6.1.3.1 Active Loop Card (فعال کردن لوپ کارت)**

·         Loop Card 1

·         Loop Card 2

·         Loop Card 3

·         Loop Card 4

با استفاده از این قابلیت، امکان فعال‌سازی هر کارت لوپ از طریق چک‌باکس فراهم شده است. در صورتی که نخواهید کارت لوپ فعال باشد، کافی است تیک مربوط به آن را بردارید.

**دکمه‌ها:**

·         **Read (خواندن):** با انتخاب این دکمه، نرم‌افزار کلیه اطلاعات و تنظیماتی را که قبلاً به‌صورت دستی از طریق خود پنل اعلام حریق وارد شده‌اند، مستقیماً از پنل خوانده و در محیط نرم‌افزار نمایش می‌دهد. این گزینه برای مشاهده و همگام‌سازی تنظیمات فعلی پنل با نرم‌افزار استفاده می‌شود.

·         **Save (ذخیره):** با انتخاب این دکمه، تمامی تغییرات و تنظیماتی که کاربر در نرم‌افزار اعمال کرده است، در پنل اعلام حریق ذخیره شده و جایگزین تنظیمات قبلی می‌شود.

**نکته :** پس از هر عملیات، پیام پاپ‌آپ موفقیت یا خطا نمایش داده می‌شود.

 

 

 

#### **6.1.3.2 Network (شبکه)**

در این تب ما می توانیم تنظیمات شبکه را انجام داد.

·         Network Status: Enable / Disable (پشتیبانی تا ۸ پنل): فعال یا غیرفعال کردن شبکه کردن پنل ها

·         Master Silence:

o       Send: Enable / Disable

o       Accept: Enable / Disable

·         Master Evacuate:

o       Send: Enable / Disable

o       Accept: Enable / Disable

·         Network Output Configuration:

o       Fire Output: Enable / Disable

o       Fault Output: Enable / Disable

o       Supervisory Output: Enable / Disable

o       NAC’s: Enable / Disable

**دکمه‌ها:**

·         **Read (خواندن):** با انتخاب این دکمه، نرم‌افزار کلیه اطلاعات و تنظیماتی را که قبلاً به‌صورت دستی از طریق خود پنل اعلام حریق وارد شده‌اند، مستقیماً از پنل خوانده و در محیط نرم‌افزار نمایش می‌دهد. این گزینه برای مشاهده و همگام‌سازی تنظیمات فعلی پنل با نرم‌افزار استفاده می‌شود.

·         **Save (ذخیره):** با انتخاب این دکمه، تمامی تغییرات و تنظیماتی که کاربر در نرم‌افزار اعمال کرده است، در پنل اعلام حریق ذخیره شده و جایگزین تنظیمات قبلی می‌شود.

**نکته :** پس از هر عملیات، پیام پاپ‌آپ موفقیت یا خطا نمایش داده می‌شود.

### **6.1.4 Night Mode (حالت شب و روز)**

**·**         **Daily Day/Night:**

o       Status**:** Enable / Disable

o       Start Time

o       End Time

**·**         **Weekend:**

o       Status**:** Enable / Disable

o       Day 1

o       Day 2

**·**         **Holiday:**

o       Status**:** Enable / Disable

o       Date **(**انتخاب تاریخ)

o       Add**:** در صورتی که کاربر یک تاریخ را انتخاب کرده باشد، با فشردن این دکمه، تاریخ انتخاب‌شده به لیست تعطیلات اضافه می‌شود.

o       Delete**:** در صورتی که کاربر یک تاریخ را از لیست تعطیلات انتخاب کند، با فشردن این دکمه، تاریخ انتخاب‌شده از لیست حذف می‌شود.  
**دکمه‌ها:**  
·         **Read (خواندن):** با انتخاب این دکمه، نرم‌افزار کلیه اطلاعات و تنظیماتی را که قبلاً به‌صورت دستی از طریق خود پنل اعلام حریق وارد شده‌اند، مستقیماً از پنل خوانده و در محیط نرم‌افزار نمایش می‌دهد. این گزینه برای مشاهده و همگام‌سازی تنظیمات فعلی پنل با نرم‌افزار استفاده می‌شود.

·         **Save (ذخیره):** با انتخاب این دکمه، تمامی تغییرات و تنظیماتی که کاربر در نرم‌افزار اعمال کرده است، در پنل اعلام حریق ذخیره شده و جایگزین تنظیمات قبلی می‌شود.

·         **Delete All:** در صورتی که کاربر قصد داشته باشد تمامی اطلاعات و تنظیمات ثبت‌شده در این بخش را به‌طور کامل حذف کند، با انتخاب این دکمه همه داده‌ها پاک شده و وضعیت به حالت اولیه بازمی‌گردد  
پس از هر عملیات، پیام پاپ‌آپ موفقیت یا خطا نمایش داده می‌شود.

## **6.2. Loop card (کارت لوپ)**  

در این صفحه می‌توان علاوه بر انتخاب کارت لوپ مورد نظر (با استفاده از چک‌باکس)، اطلاعات داخل هر لوپ کارت را اضافه یا ویرایش کرد.

**کارت‌های لوپ:**

1\.      LoopCard1  
2\.      LoopCard2  
3\.      LoopCard3  
4\.      LoopCard4

با انتخاب هر لوپ کارت، می‌توان اطلاعات آن شامل نوع دیوایس‌ها، تعداد دیوایس‌های موجود و مشخصات هر دیوایس را مشاهده و تغییر داد.

**دکمه‌ها:**

1\.      **Read Device (شناسایی خودکار):** با انتخاب این دکمه اطلاعات کامل دیوایس‌های نصب‌شده روی لوپ کارت را با تمامی ویژگی‌ها و تعداد آنها به صورت لیستی نمایش می‌دهد.

***دیوایس‌های نمایش داده شده شامل:***

·         Smoke Detector (دتکتور دود)

·         Heat Detector (دتکتور حرارت)

·         Multi Detector (دتکتور ترکیبی)

·         Manual Call Point (شستی اعلام حریق)

·         Sounder/Output (آژیر/خروجی)

·         Zone Module (ماژول زون)

·         Input Module (ماژول ورودی)

**·**         Input/output Module (ماژول ورودی/خروجی)

2\.      **Add Device (اضافه کردن دیوایس):** انتخاب نوع دیوایس از لیست و اضافه کردن آن به لیست دیوایس‌ها.  
3\.      **Filter Device (فیلتر دیوایس):** نمایش فقط دیوایس‌هایی که نوع آنها انتخاب شده است.  
4\.      **Show All (نمایش همه):** نمایش تمامی دیوایس‌ها.  
5\.      **Save (ذخیره):** ذخیره تمامی تغییرات اعمال شده.  
6\.      **Delete (حذف):** حذف دیوایس انتخاب شده از لیست.  
7\.      **Delete All (حذف همه):** حذف تمامی دیوایس‌ها از لیست.  
8\.      **Print (چاپ):** چاپ اطلاعات لوپ کارت.

 

 

 

**ویژگی‌های قابل ویرایش دیوایس‌ها از داخل لیست :**

**1\.**      **Status (وضعیت): فعال یا غیرفعال**  
·         Enable (فعال)  
·         Disable (غیرفعال)

**2\.**      **Style (نوع سیم کشی):**  
·         Class B (کلاس B)  
·         Class A (کلاس A)

**3\.**      **Device List (لیست دیوایس‌ها): شامل مشخصات و ویژگی‌های زیر:**  
·         Number Device (شماره دیوایس)  
·         Status (وضعیت): در این بخش، امکان فعال یا غیرفعال‌سازی این دیوایس در پنل اعلام حریق وجود دارد.  
o   	Enable (فعال)  
o   	Disable (غیرفعال)  
·         Device (نوع دیوایس): نوع دستگاه مورد نظر را می توان انتخاب کرد.  
·         Smoke Detector (دتکتور دود)

·         Heat Detector (دتکتور حرارت)

·         Multi Detector (دتکتور ترکیبی)

·         Manual Call Point (شستی اعلام حریق)

·         Sounder/Output (آژیر/خروجی)

·         Zone Module (ماژول زون)

·         Input Module (ماژول ورودی)

**·**         Input/output Module (ماژول ورودی/خروجی)  
·         Category (دسته‌بندی): براساس نوع دیوایس دسته بندی انتخاب میشود.  
o   	Relay

o   	Control

o   	Other

o   	CL-B-S-B

o   	CL-B-S-C

**·**         **IP**

**·**         **Deactivation**

·         **Sensitivity (نوع حسایت)**

o   	Low (کم)

o   	Medium (متوسط)

o   	High (زیاد)

·         **Night Mode (حالت کارکرد)**

o   	Day (روز)

o   	Night (شب)

·         **Location (آدرس):** آدرس دقیق مکان دیوایس نوشته می شود.

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

## **6.3. Group (گروهبندی)**  

جهت گروه‌بندی زون استفاده می‌شود و شامل دو زیرتب است:

### **6.3.1 Zone Group  (گروهبندی زون)**

**امکانات**:

1\.      **Read All Groups  (خواندن همه گروه‌ها) :** با استفاده از این گزینه، در صورتی که قبلاً گروه‌بندی‌ها از طریق خود پنل اعلام حریق انجام شده باشد، کاربر می‌تواند تمامی اطلاعات گروه‌ها را از پنل خوانده و در نرم‌افزار مشاهده کند.  
 در صورت برقراری صحیح ارتباط بین پنل و کامپیوتر، اطلاعات ذخیره‌شده در پنل به‌صورت کامل در نرم‌افزار نمایش داده می‌شود.  
 در صورتی که ارتباط برقرار نباشد، پیام **قطع ارتباط با پنل** به کاربر نمایش داده خواهد شد.

2\.  	**Group Number  (شماره گروه):** از طریق این لیست بازشونده، کاربر می‌تواند شماره گروه مورد نظر را انتخاب کند.  
 امکان ایجاد گروه‌ها از شماره **1 تا 64** وجود دارد.

3\.  	**Loop Number  (شماره لوپ کارت) :** با انتخاب لوپ کارت مورد نظر از لیست بازشونده، دیوایس‌هایی که قبلاً در بخش  **Loop Card** آدرس‌دهی شده‌اند، در قسمت **Input Devices** نمایش داده می‌شوند.  
 این لیست شامل شماره دیوایس و نام دیوایس‌های موجود در لوپ کارت انتخاب‌شده است.

4\.  	**Input Devices  (لیست دیوایس‌های ورودی):** پس از نمایش لیست دیوایس‌ها، کاربر می‌تواند با کلیک روی هر دیوایس، آن را انتخاب کند.  
 با انتخاب دیوایس، مشخصات آن شامل **شماره لوپ کارت و شماره دیوایس** به‌صورت خودکار به لیست **Zone Grouped Devices List**  اضافه می‌شود.  
 پس از اضافه شدن دیوایس به گروه، آن دیوایس از لیست Input Devices حذف می‌شود تا از انتخاب مجدد آن در همان گروه جلوگیری شود.

5\.  	**Pre Alarm  (پیش هشدار) :** با استفاده از این گزینه، کاربر می‌تواند قابلیت پیش‌هشدار را برای گروه ایجادشده در لیست **Zone Grouped Devices List**  فعال یا غیرفعال کند.  
 این تنظیم از طریق چک‌باکس‌های **Enable / Disable**   انجام می‌شود.

**دکمه های مدیریتی:**

6\.  	**Delete (حذف ردیف انتخاب شده):** در صورتی که یک ردیف از لیست **Zone Grouped Devices List** انتخاب شده باشد، با فشردن این دکمه، آن ردیف از لیست حذف می‌شود.

7\.  	**Delete All (حذف همه گروه‌ها):** با انتخاب این گزینه، تمامی گروه‌های ایجادشده در لیست **Zone Grouped Devices List** به‌طور کامل حذف می‌شوند.

8\.  	**Save (ذخیره گروه‌ها):** با استفاده از این گزینه، تمامی گروه‌بندی‌های ایجادشده در نرم‌افزار ذخیره می‌شوند، اما هنوز به پنل منتقل نخواهند شد.

9\.  	**Update All (به‌روزرسانی گروه‌ها در پنل):** در صورتی که ارتباط پنل با کامپیوتر به‌درستی برقرار باشد، با انتخاب این گزینه، تمامی اطلاعات و گروه‌بندی‌های انجام‌شده در نرم‌افزار به پنل اعلام حریق منتقل و اعمال می‌شوند.

 

### **6.3.2 Input & Output Group  (گروهبندی ورودی و خروجی)**

در این بخش امکان ایجاد، ویرایش و مدیریت گروه‌بندی دیوایس‌های ورودی و خروجی وجود دارد.

#### **6.3.2.1 انتخاب شماره گروه‌ها**

1\.      **Input Group Number (شماره گروه ورودی):**  
 با استفاده از لیست بازشونده، می‌توان شماره گروه ورودی مورد نظر را انتخاب کرد.  
 امکان انتخاب شماره گروه از **1 تا 64** وجود دارد.

2\.      **Output Group Number (شماره گروه خروجی):**  
 با استفاده از لیست بازشونده، می‌توان شماره گروه خروجی مورد نظر را انتخاب کرد.  
 امکان انتخاب شماره گروه از **1 تا 64** وجود دارد.

پس از انتخاب شماره گروه‌های ورودی و خروجی، تنظیمات گروه‌بندی از طریق بخش **Input / Output Grouping**  انجام می‌شود.

1\.      **Input Group Number (شماره گروه ورودی) :**  
 با استفاده از لیست بازشونده، می‌توان شماره گروه ورودی مورد نظر را انتخاب کرد.  
 امکان انتخاب شماره گروه از **1 تا 64** وجود دارد.

2\.      **Output Group Number  (شماره گروه خروجی) :**  
 با استفاده از لیست بازشونده، می‌توان شماره گروه خروجی مورد نظر را انتخاب کرد.  
 امکان انتخاب شماره گروه از **1 تا 64** وجود دارد.

#### **6.3.2.2 Input / Output Grouping**

##### **6.3.2.2.1. Loop Number (شماره لوپ):**

با استفاده از لیست بازشونده، شماره لوپ کارت مورد نظر انتخاب می‌شود تا دیوایس‌های مربوط به آن لوپ کارت گروه‌بندی شوند. پس از انتخاب لوپ کارت:

·         تمامی دیوایس‌هایی که قبلاً در بخش **Loop Card**  آدرس‌دهی شده‌اند، به‌صورت خودکار در دو لیست مجزا نمایش داده می‌شوند:

o        **Input Devices** (دیوایس‌های ورودی)

o        **Output Devices** (دیوایس‌های خروجی)

تمامی دیوایس‌های ورودی در لیست Input Devices و تمامی دیوایس‌های خروجی در لیست Output Devices نمایش داده می‌شوند.

**نحوه ی انتخاب دیوایس‌ها برای گروه‌بندی :**

کاربر می‌تواند دیوایس‌ها را به دو روش به گروه‌بندی اضافه کند:

·         **روش اول – انتخاب دستی:**  
 با کلیک روی شماره هر دیوایس در لیست Input Devices یا Output Devices، دیوایس انتخاب‌شده به لیست گروه‌بندی اضافه می‌شود.

·         **روش دوم – انتخاب گروهی:**  
 در صورتی که کاربر بخواهد تمامی دیوایس‌های نمایش‌داده‌شده در لیست Input Devices یا Output Devices را به‌صورت یکجا به گروه‌بندی اضافه کند، کافی است روی دکمه‌ای که در زیر لیست دیوایس‌ها با نماد **«\<\<»** مشخص شده است کلیک کند.  
 با این کار، تمامی دیوایس‌ها به لیست گروه‌بندی منتقل می‌شوند.

**نکته:** پس از اضافه شدن هر دیوایس به گروه‌بندی، شماره آن دیوایس از لیست دیوایس‌ها حذف شده و به لیست گروه‌بندی منتقل می‌شود تا از انتخاب مجدد آن جلوگیری شود.

##### **6.3.2.2.2.Active Count (تعداد فعال) :**

این گزینه مخصوص دیوایس‌های ورودی است و تعداد دیوایس‌های فعال را مشخص می‌کند.  
 مقدار این گزینه می‌تواند از **1 تا تعداد دیوایس‌های انتخاب‌شده** تنظیم شود.

##### **6.3.2.2.3.Output Active for (فعال‌سازی خروجی برای):**

این بخش جهت تعیین شرایط فعال شدن خروجی‌ها استفاده می‌شود و دارای لیست بازشونده با گزینه‌های زیر است:

·         Fire

·         Supervisory

·         Fault

·         Reset

·         Pre Alarm

##### **6.3.2.2.4. Delay (0–999 Sec) (تاخیر):**

از این گزینه برای ایجاد تاخیر زمانی در فعال شدن خروجی‌ها استفاده می‌شود.  
 مقدار تاخیر قابل تنظیم از **0  تا 999 ثانیه** است.

##### **6.3.2.2.5. Status(وضعیت):**

این بخش جهت فعال یا غیرفعال‌سازی گروه‌بندی استفاده می‌شود:

·  Enable (فعال)  
·  Disable (غیرفعال)

##### **6.3.2.2.6. Pre View (پیش نمایش):**

با انتخاب دکمه Pre View، پیش‌نمایشی از گروه‌بندی‌های ورودی و خروجی به‌صورت یکجا در لیست نمایش داده می‌شود.

#### **6.3.2.3 دکمه های مدیریتی**

##### **6.3.2.3.1. Read All Groups (خواندن همه گروه‌بندی‌ها):**

در صورتی که قبلاً گروه‌بندی‌ها از طریق پنل انجام شده باشد و ارتباط پنل با کامپیوتر به‌درستی برقرار باشد، با انتخاب این دکمه اطلاعات گروه‌بندی از داخل پنل خوانده شده و جهت مشاهده و ویرایش در نرم‌افزار نمایش داده می‌شود.  
 در صورت عدم برقراری ارتباط، پیام **عدم دسترسی یا قطع اتصال** نمایش داده می‌شود.

##### **6.3.2.3.2. Save (ذخیره):**

پس از انجام گروه‌بندی مورد نظر، با انتخاب این دکمه اطلاعات درون نرم‌افزار ذخیره می‌شود.

##### **6.3.2.3.3. Delete (حذف):**

با انتخاب ردیف مورد نظر از لیست گروه‌بندی و کلیک روی این دکمه، ردیف انتخاب‌شده حذف می‌شود.

##### **6.3.2.3.4. Delete All (حذف همه):**

با انتخاب این دکمه، تمامی اطلاعات مربوط به گروه‌بندی‌ها حذف می‌گردد.

##### **6.3.2.3.5. Update All (به روزرسانی همه):**

با انتخاب این گزینه، تمامی گروه‌بندی‌های ایجادشده در نرم‌افزار به پنل اعلام حریق منتقل و اعمال می‌شوند.

##### **6.3.2.3.6. Print (چاپ):**

با انتخاب این گزینه، امکان چاپ تمامی اطلاعات مربوط به گروه‌بندی‌های ورودی و خروجی فراهم می‌شود.

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

## **6.4. Features (ویژگی ها):**

در این بخش امکان فعال یا غیرفعال‌سازی برخی از ویژگی‌های عملکردی پنل اعلام حریق وجود دارد. کاربر می‌تواند با استفاده از این تنظیمات، رفتار سیستم را مطابق با نیاز پروژه تنظیم کند (مطابق تصویر مربوطه).

### **6.4.1. تنظیمات ویژگی‌ها**

**1\. Ac Loss Delay  (تأخیر در اعلام قطع برق):**  
 در صورت فعال‌سازی این گزینه، می‌توان برای اعلام قطع برق ورودی، یک تأخیر زمانی تعریف کرد.  
 مقدار این تأخیر قابل تنظیم از **0 تا 999 دقیقه** است.  
**2.Trouble Reminder (یادآوری خطاها / اتفاقات):**  
 در صورت فعال‌سازی این گزینه، سیستم می‌تواند پیام‌های یادآوری مربوط به خطاها یا اتفاقات را در بازه زمانی مشخص‌شده تکرار کند.  
 زمان یادآوری قابل تنظیم از **0 تا 999 دقیقه** می‌باشد.  
**3.Auto Silence (خاموش‌سازی خودکار صدا):**  
 با فعال کردن این گزینه، صدای هشدار سیستم پس از گذشت زمان تعیین‌شده به‌صورت خودکار قطع می‌شود.  
 زمان خاموش‌سازی خودکار قابل تنظیم از **0 تا 999 ثانیه** است.  
**4.Silence Inhibit (جلوگیری از قطع صدا):**  
 در صورت فعال‌سازی این گزینه، امکان قطع صدای هشدار برای مدت زمان مشخصی غیرفعال می‌شود.  
 مدت زمان جلوگیری از قطع صدا قابل تنظیم از **0 تا 999 ثانیه** است.

### **6.4.2. دکمه‌های موجود در تب Features**

**1.Save (ذخیره):**  
 با انتخاب این گزینه، تمامی اطلاعات و تنظیمات واردشده در این بخش در نرم‌افزار ذخیره می‌شوند.  
**2.Read (خواندن):**  
 با انتخاب این گزینه، در صورتی که پنل به کامپیوتر متصل باشد، اطلاعات و تنظیمات فعلی از داخل پنل خوانده شده و در نرم‌افزار نمایش داده می‌شود.

## **6.5. Events (رویدادها):**

در این بخش امکان مشاهده، مدیریت و تحلیل تاریخچه رویدادها و خطاهای ثبت‌شده در پنل اعلام حریق فراهم می‌باشد. کاربر می‌تواند رویدادها را مشاهده، حذف، چاپ و یا از آن‌ها خروجی تهیه کند. همچنین این بخش با نقشه دوبعدی (2D) یا سه‌بعدی (3D) پروژه یکپارچه بوده و نمایش بصری رویدادها بر روی نقشه امکان‌پذیر است (مطابق تصویر مربوطه).

### **6.5.1 امکانات و گزینه‌ها**

**1.Get Events  (دریافت رویدادها):**  
 با انتخاب این دکمه، گزارش رویدادهای ثبت‌شده در پنل شامل **تاریخ، زمان و نوع رویداد** از پنل خوانده شده و در نرم‌افزار به‌صورت لیست نمایش داده می‌شود.

**2.Clear Events  (پاک کردن رویدادها):**  
 با انتخاب این گزینه، تمامی رویدادهای نمایش‌داده‌شده در لیست از محیط نرم‌افزار حذف می‌شوند. (دسترسی مدیر)

**3.Print  (چاپ):**  
 با انتخاب این گزینه، امکان چاپ تمامی رویدادهای نمایش‌داده‌شده در لیست فراهم می‌شود.

**4.Export  (خروجی گرفتن):**  
 با استفاده از این گزینه، کاربر می‌تواند از رویدادهای ثبت‌شده خروجی تهیه کند و آن‌ها را در قالب فایل ذخیره نماید.

 

### **6.5.2 نمایش رویدادها بر روی نقشه (2D / 3D)**

در این بخش، رویدادها علاوه بر نمایش در قالب جدول، به‌صورت گرافیکی بر روی نقشه دوبعدی یا سه‌بعدی پروژه نمایش داده می‌شوند. نقشه در قسمت بالای صفحه قرار داشته و لیست کامل رویدادها در بخش پایینی صفحه نمایش داده می‌شود.

**امکانات این بخش شامل موارد زیر است:**

·         نمایش محل وقوع آلارم‌ها و خطاها بر روی نقشه پروژه

·         تفکیک نوع رویدادها با رنگ‌بندی مشخص:

o        **حریق (Fire Alarm):** نمایش به رنگ قرمز

o        **خطا (Fault):** نمایش به رنگ زرد

·         قابلیت انتخاب **تاریخ شروع** و **تاریخ پایان** جهت تعیین بازه زمانی نمایش رویدادها

·         دکمه **تأیید (Apply / Confirm)** برای اعمال بازه زمانی انتخاب‌شده

پس از تأیید بازه زمانی، تنها رویدادهای مربوط به آن بازه بر روی نقشه و در جدول پایین صفحه نمایش داده می‌شوند.

### **6.5.3 جزئیات تعاملی رویدادها روی نقشه**

·         با قرار گرفتن نشانگر ماوس بر روی هر ناحیه یا المان مشخص‌شده روی نقشه، اطلاعات خلاصه رویداد شامل:

o        تاریخ

o        زمان

o        نوع رویداد

o        توضیحات کوتاه  
 به‌صورت Tooltip نمایش داده می‌شود.

·         با انتخاب (کلیک) هر رویداد روی نقشه، اطلاعات کامل آن در جدول رویدادهای پایین صفحه برجسته یا فیلتر می‌شود.

### **6.5.4 ثبت و نمایش اقدامات کاربران**

در جدول رویدادها، علاوه بر اطلاعات فنی هر رویداد، امکان نمایش سوابق مدیریتی و عملیاتی نیز وجود دارد، از جمله:

·         نام کاربر یا اپراتوری که رویداد را مشاهده یا بررسی کرده است

·         اقدام انجام‌شده توسط کاربر (تأیید، بررسی، رفع خطا، ریست و …)

·         تاریخ و زمان انجام هر اقدام

این بخش امکان رهگیری کامل رویدادها و پاسخ کاربران به خطاها و آلارم‌ها را فراهم می‌کند و نقش مهمی در تحلیل عملکرد سیستم و اپراتورها دارد.

 

 

 

 

 

 

## **6.6Remote Panel  (ریموت پنل)**

این بخش امکان اتصال و مدیریت چند پنل اعلام حریق را به‌صورت ریموت و متمرکز فراهم می‌کند. کاربر می‌تواند با انتخاب شماره پنل مورد نظر، ارتباط نرم‌افزار را با پنل مقصد برقرار کرده و وضعیت آن را مشاهده و مدیریت نماید.

### **6.6.1 انتخاب و اتصال پنل‌ها**

در این بخش، کاربر می‌تواند از طریق گزینه **Number of Panel**  شماره پنل مورد نظر را در بازه**1  تا 8** انتخاب نماید.

·         پس از انتخاب شماره پنل، با فشردن دکمه **Synchronous With Panel**، فرآیند همگام‌سازی بین نرم‌افزار و پنل انتخاب‌شده آغاز می‌شود.

·         در صورتی که اتصال سخت‌افزاری و تنظیمات ارتباطی به‌درستی انجام شده باشد، ارتباط ریموت برقرار شده و پنل‌ها با یکدیگر همگام می‌شوند.

·         وضعیت موفق یا ناموفق بودن اتصال به‌صورت پیام سیستمی به کاربر نمایش داده می‌شود.  
 (مطابق تصویر مربوطه)

###  **6.6.2 نمایش وضعیت پنل ریموت**

پس از برقراری ارتباط موفق، اطلاعات زیر از پنل ریموت دریافت و نمایش داده می‌شود:

·         وضعیت کلی پنل (Normal / Alarm / Fault)

·         وضعیت ارتباط (Connected / Disconnected)

·         آخرین زمان همگام‌سازی

·         تعداد رویدادهای فعال (حریق یا خطا)

این اطلاعات به‌صورت لحظه‌ای یا با هر بار همگام‌سازی به‌روزرسانی می‌شوند.

### **6.6.3 مدیریت هم‌زمان چند پنل**

نرم‌افزار امکان مدیریت هم‌زمان چند پنل متصل را فراهم می‌کند، به‌طوری که:

·         هر پنل با شماره اختصاصی خود شناسایی می‌شود.

·         امکان جابه‌جایی سریع بین پنل‌ها بدون نیاز به قطع ارتباط وجود دارد.

·         رویدادهای هر پنل به‌صورت مجزا ثبت و قابل تفکیک هستند.

### **6.6.4 همگام‌سازی اطلاعات (Synchronization)**

در فرآیند همگام‌سازی، اطلاعات زیر بین نرم‌افزار و پنل ریموت تبادل می‌شود:

·         رویدادها و خطاها

·         وضعیت ورودی‌ها و خروجی‌ها

·         وضعیت تجهیزات متصل

·         زمان و تاریخ سیستم پنل (در صورت مجاز بودن)

در صورت بروز خطا در همگام‌سازی، پیام هشدار مناسب به کاربر نمایش داده می‌شود.

### **6.6.5 ثبت و گزارش‌گیری ارتباطات**

نرم‌افزار کلیه عملیات مربوط به اتصال ریموت را ثبت می‌کند، از جمله:

·         زمان شروع و پایان اتصال

·         نام کاربر انجام‌دهنده عملیات

·         نتیجه همگام‌سازی (موفق / ناموفق)

·         علت قطع ارتباط (در صورت وجود)

این اطلاعات در بخش گزارش‌ها و رویدادها قابل مشاهده خواهند بود.

### **6.6.6 ملاحظات عملکردی و امنیتی**

·         تنها کاربران دارای سطح دسترسی مجاز امکان اتصال به پنل‌های ریموت را دارند.

·         در صورت قطع ناگهانی ارتباط، نرم‌افزار به‌صورت خودکار وضعیت پنل را به‌روز می‌کند.

·         امکان جلوگیری از اتصال هم‌زمان چند کاربر به یک پنل (در صورت نیاز پروژه) وجود دارد.

 

 

 

## **6.7Report (گزارش)**

این بخش به‌منظور مشاهده، تحلیل و مستندسازی گزارش‌های عملکردی نرم‌افزار اعلام حریق طراحی شده است. کاربر می‌تواند گزارش‌های مربوط به بخش‌های مختلف سیستم را مشاهده کرده، آن‌ها را چاپ نماید یا از اطلاعات موجود خروجی تهیه کند. (مطابق تصویر مربوطه)  
با انتخاب هر یک از گزینه‌های زیر و فشردن دکمه **Generate**، گزارش مربوطه تولید و در محیط نرم‌افزار نمایش داده می‌شود.

### **6.7.1 Devices Not Configured in any Group**

 (دستگاه‌های پیکربندی‌نشده در هیچ گروه)  
در این بخش، با کلیک بر روی دکمه **Generate**، لیستی از دستگاه‌هایی که در هیچ‌یک از گروه‌های تعریف‌شده پیکربندی نشده‌اند نمایش داده می‌شود. این گزارش به شناسایی تجهیزات ناقص یا فراموش‌شده در فرآیند پیکربندی کمک می‌کند.

### **6.7.2 Devices Not Configured in any Group**

 (دستگاه‌های پیکربندی‌نشده در هیچ گروه)  
در این بخش، با کلیک بر روی دکمه **Generate**، لیستی از دستگاه‌هایی که در هیچ‌یک از گروه‌های تعریف‌شده پیکربندی نشده‌اند نمایش داده می‌شود. این گزارش به شناسایی تجهیزات ناقص یا فراموش‌شده در فرآیند پیکربندی کمک می‌کند.

### **6.7.3 Configured Groups**

 (گروه‌های پیکربندی‌شده)  
در این بخش، با کلیک بر روی دکمه **Generate**، لیستی از گروه‌های تعریف‌شده به همراه اطلاعات مربوط به هر گروه نمایش داده می‌شود. این گزارش برای بررسی ساختار گروه‌بندی، کنترل صحت تنظیمات و مستندسازی سیستم کاربرد دارد.

### **6.7.4 چاپ و خروجی گزارش‌ها**

·       با استفاده از دکمه **Print (چاپ)**، کاربر می‌تواند گزارش‌های نمایش‌داده‌شده را چاپ نماید.

·       با استفاده از دکمه **Export(خروجی)**، امکان تهیه خروجی از گزارش‌ها و ذخیره آن‌ها در قالب فایل فراهم می‌شود.

## **6.8 GSM  (تلفن‌کننده)**

این بخش به‌منظور پیکربندی، مدیریت و نظارت بر عملکرد **تلفن‌کننده GSM پنل اعلام حریق** طراحی شده است. از طریق این قسمت، کاربر می‌تواند تنظیمات مربوط به پروژه، اطلاعات پنل، رویدادها و نحوه ارسال هشدارها از طریق **پیامک (SMS)** و **تماس صوتی (Voice Call)** را انجام دهد.

### **6.8.1 مدیریت پروژه (Project Management)**

در این بخش، اطلاعات پایه مربوط به پروژه و سیم‌کارت تلفن‌کننده تعریف می‌شود.

1\.      **نام پروژه (Project Name)**

·         تعیین نام پروژه جهت شناسایی در نرم‌افزار و گزارش‌ها

2\.      **سیم‌کارت (SIM Card)**

·         شماره سیم‌کارت مورد استفاده

·         اپراتور سیم‌کارت

·         وضعیت آنتن‌دهی و اتصال به شبکه (در صورت پشتیبانی)

### **6.8.3 مدیریت رویدادها (Event Management)**

این بخش مربوط به تعریف رویدادهایی است که در صورت وقوع، از طریق تلفن‌کننده اطلاع‌رسانی انجام می‌شود.

#### **6.8.3.1 افزودن رویداد (Add Event)**

برای هر رویداد، تنظیمات زیر قابل تعریف است:

·         **شماره موبایل دریافت‌کننده**

·         امکان تعریف **حداکثر 10 شماره موبایل**

·         قابلیت فعال یا غیرفعال کردن هر شماره

·         **متن پیامک (SMS Text)**

·         تعریف متن پیامک ارسالی

·         امکان تفکیک متن برای رویدادهای:

o       Fire (حریق)

o       Fault (خطا)

·         امکان استفاده از اطلاعات پروژه (نام پروژه، نام پنل، آدرس)

·         **پیام صوتی (Voice Message)**

·         پخش پیام صوتی از طریق تماس تلفنی

·         امکان انتخاب یا ضبط پیام صوتی مجزا برای:

o       حریق

o       خطا

#### **6.8.3.2 نوع رویدادهای قابل ارسال**

نرم‌افزار باید امکان ارسال هشدار برای رویدادهای زیر را داشته باشد:

·         حریق (Fire Alarm)

·         خطا (Fault)

·         قطع برق

·         قطع باتری

·         باز شدن یا دستکاری پنل (Tamper)

·         قطع ارتباط GSM (در صورت پشتیبانی)

###  **6.8.4 اتصال و ارتباط (Communication)**

این بخش به‌منظور برقراری ارتباط بین نرم‌افزار و تلفن‌کننده GSM پنل اعلام حریق طراحی شده است. ارتباط می‌تواند به‌صورت **محلی (Local)** یا **از راه دور (Remote)** برقرار شود تا کاربر بتواند متناسب با شرایط پروژه، تنظیمات را انجام داده و وضعیت تلفن‌کننده را مدیریت نماید.

####  **6.8.4.1 اتصال بلوتوث (Bluetooth Connection)**

در این حالت، ارتباط بین نرم‌افزار و تلفن‌کننده به‌صورت محلی و از طریق بلوتوث برقرار می‌شود.

·         برقراری ارتباط مستقیم بین نرم‌افزار و تلفن‌کننده از طریق بلوتوث

·         نمایش وضعیت اتصال (Connected / Disconnected)

·         امکان تست ارتباط بلوتوث

·         ارسال تنظیمات از نرم‌افزار به تلفن‌کننده

·         دریافت تنظیمات فعلی تلفن‌کننده و نمایش آن‌ها در نرم‌افزار

این روش ارتباطی معمولاً در زمان نصب، راه‌اندازی اولیه و تنظیمات محلی مورد استفاده قرار می‌گیرد.

#### **6.8.4.2 اتصال از طریق اینترنت (Internet Connection)**

در این حالت، کاربر می‌تواند بدون حضور فیزیکی در محل پروژه و از طریق اینترنت به تلفن‌کننده یا پنل اعلام حریق متصل شود.

·         برقراری ارتباط نرم‌افزار با پنل یا تلفن‌کننده از طریق بستر اینترنت

·         امکان مدیریت و مشاهده تنظیمات از راه دور

·         نمایش وضعیت آنلاین یا آفلاین بودن دستگاه

·         امکان تست ارتباط اینترنتی

·         دریافت رویدادها، وضعیت‌ها و تنظیمات از راه دور

این قابلیت به کاربر اجازه می‌دهد در هر مکان، بدون نیاز به حضور در محل، سیستم اعلام حریق را پایش و مدیریت کند.

#### **6.8.4.3 همگام‌سازی تنظیمات (Synchronization)**

در هر دو حالت ارتباطی (بلوتوث یا اینترنت)، امکان همگام‌سازی تنظیمات فراهم است:

·         **ارسال تنظیمات از نرم‌افزار به تلفن‌کننده**

·         **دریافت تنظیمات فعلی از تلفن‌کننده به نرم‌افزار**

·         نمایش وضعیت موفق یا ناموفق بودن فرآیند همگام‌سازی

همگام‌سازی تضمین می‌کند که تنظیمات نمایش‌داده‌شده در نرم‌افزار با وضعیت واقعی دستگاه یکسان باشد.

#### **6.8.4.4 ملاحظات امنیتی ارتباط**

·         دسترسی از طریق اینترنت فقط برای کاربران مجاز امکان‌پذیر است

·         ارتباطات باید به‌صورت امن و رمزنگاری‌شده انجام شود

·         ثبت لاگ کلیه اتصال‌ها (محلی و از راه دور) شامل تاریخ، زمان و نام کاربر

 

 

 

 

## **6.9Monitoring  (مانیتورینگ)**

بخش مانیتورینگ نرم‌افزار اعلام حریق به‌منظور **پایش لحظه‌ای وضعیت سیستم، نمایش دیداری تجهیزات و مدیریت رویدادها** طراحی شده است. این بخش قابلیت اجرا بر روی کامپیوتر، تبلت و تلفن همراه را داشته و امکان مانیتورینگ **محلی و از راه دور** را فراهم می‌کند.

در این بخش، پلان‌های ساختمانی به‌صورت **دو‌بعدی (2D)** و **سه‌بعدی (3D)** تعریف شده و کلیه تجهیزات اعلام حریق شامل دتکتورها، Manual Call Point (MCP)، آژیرها و سایر تجهیزات بر روی نقشه جانمایی می‌شوند. در صورت وقوع هرگونه رویداد مانند حریق یا خطا، محل دقیق آن به‌صورت **نمایش بصری روی نقشه** مشخص می‌شود.

با انتخاب گزینه "**مانیتورینگ"** از منوی اصلی نرم‌افزار، یک **صفحه اختصاصی** در پایین منوی اصلی (شامل تنظیمات پنل و سایر بخش‌ها) باز می‌شود. این صفحه دارای **منوی اختصاصی مانیتورینگ** بوده و در عین حال، **دسترسی کامل به منوی اصلی نرم‌افزار مدیریت پنل** حفظ می‌شود.

### **6.9.1. منوی اختصاصی نرم‌افزار مانیتورینگ**

نرم‌افزار مانیتورینگ به‌صورت **ماژولار** طراحی شده و دارای منوی اختصاصی زیر است که هر بخش وظیفه مشخصی را بر عهده دارد:

**6.9.1.1 مدیریت ارتباطات**

·         پروتکل ارتباطی (سریال، شبکه محلی و اینترنت)

·         مانیتورینگ وضعیت اتصال (متصل / قطع)

·         همگام‌سازی اطلاعات با پنل

**6.9.1.2 نمایش گرافیکی سیستم** **(Visualization)**

·         مشاهده نقشه‌های دوبعدی و سه‌بعدی ساختمان

·         نمایش موقعیت دتکتورها، آژیرها و تجهیزات

·         نمایش لحظه‌ای وضعیت تجهیزات روی نقشه

**6.9.1.3. مدیریت رویدادها (Event Management)**

·         دریافت و نمایش لحظه‌ای رویدادهای حریق و خطا

·         نمایش شماتیکی و تفکیک رنگی رویدادها روی نقشه

·         امکان فیلتر و جستجوی سریع در لیست رویدادها

**6.9.1.4 مدیریت گزارش‌ها (Report Management)**

·         تهیه گزارش‌های بازه‌ای و فیلترشده از وقایع پنل

·         نمایش گزارش‌ها روی نقشه‌های دوبعدی و سه‌بعدی

·         ذخیره و چاپ آسان گزارش‌ها برای مستندسازی

 

**6.9.1.5 کنترل و فرمان‌دهی از راه دور**

·         قطع صدای هشدار پنل (بازر)

·         فعال‌سازی و غیرفعال‌سازی دستی آژیرها

·         کنترل تکی، گروهی یا کلی تجهیزات

**6.9.1.6 تست و عیب‌یابی تجهیزات**

·         تست عملکرد دتکتورها و آژیرها

·         نمایش وضعیت فعال/غیرفعال تجهیزات

·         نمایش نتایج تست به‌صورت گرافیکی

با انتخاب هر یک از منوهای بالا تنظیمات آن قسمت به صورت تب در زیر آن نمایش داده می‌شود.

### **6.9.2. ساختار تب‌ها در بخش مانیتورینگ**

با ورود به بخش مانیتورینگ، **تب فعال پیش‌فرض** به‌صورت خودکار روی **«مدیریت ارتباطات»** قرار می‌گیرد تا وضعیت اتصال نرم‌افزار به پنل به‌صورت اولیه نمایش داده شود.

با انتخاب هر گزینه از **منوی اختصاصی مانیتورینگ**، اطلاعات و تنظیمات مربوط به همان بخش در قالب یک **تب** در ناحیه کاری مانیتورینگ که **در زیر منوی اختصاصی مانیتورینگ** قرار دارد، نمایش داده می‌شود.

**ویژگی تب ها :**

·         با انتخاب هر گزینه از **منوی اختصاصی مانیتورینگ**، یک **تب جدید** در پایین منوی اصلی نرم‌افزار باز می‌شود.  
·         هر تب دارای یک **نوار عنوان** در بخش بالایی خود است که **عنوان منوی انتخاب‌شده در آن نمایش داده نمی‌شود** و در **سمت راست نوار عنوان، علامت بستن (×)** قرار دارد.  
·         در هر تب، **اطلاعات، وضعیت‌ها و گزینه‌های ویرایش مرتبط با همان بخش** نمایش داده می‌شود.  
·         این تب‌ها به کاربر امکان می‌دهند **به‌صورت همزمان چند بخش مختلف** را باز نگه داشته و بدون خروج از صفحه بین آن‌ها جابجا شود.  
·         **تب فعال** به‌صورت واضح از سایر تب‌ها متمایز بوده و کاربر می‌تواند با استفاده از علامت (×) تب‌ها را ببندد.  
·         در ابتدای ورود به بخش مانیتورینگ، **تنها یک تب به‌صورت پیش‌فرض باز و فعال است** که مربوط به **«مدیریت ارتباطات»** می‌باشد.  
·         در صورتی که کاربر هنگام بستن یک تب، **تغییراتی در تنظیمات انجام داده باشد که ذخیره نشده‌اند**، نرم‌افزار یک **پیغام هشدار به‌صورت پنجره پاپ‌آپ** نمایش می‌دهد و از کاربر می‌پرسد آیا مایل به **ذخیره تغییرات** است یا خیر.  
o        در صورت انتخاب گزینه **ذخیره**، تغییرات ذخیره شده و تب بسته می‌شود.  
o        در صورت انتخاب گزینه **عدم ذخیره**، تب بدون ذخیره تغییرات بسته می‌شود.  
o        در صورت **انصراف**، تب باز باقی می‌ماند.

 

 

#### **6.9.1.1 مدیریت ارتباطات**

با انتخاب دکمه یا آیکن **مدیریت ارتباطات** در نرم‌افزار، امکان تعیین نوع اتصال نرم‌افزار به پنل از طریق **دو پروتکل ارتباطی اصلی** فراهم می‌شود.

##### **6.9.1.1.1. تنظیم پروتکل ارتباطی نرم‌افزار با پنل**

در منوی **«مدیریت ارتباطات»،** زیرمنویی با عنوان **«پروتکل ارتباطی»** وجود دارد که با انتخاب آن، کاربر به صفحه تنظیمات مربوط به نوع اتصال هدایت می‌شود.

کاربر در این بخش می‌تواند **پروتکل ارتباطی مورد نظر** را انتخاب و تنظیم کند. نرم‌افزار دو روش اصلی ارتباطی را پشتیبانی می‌کند: **پورت سریال** و **پورت شبکه**.

·         **پورت سریال :(Serial)** اتصال مستقیم با استفاده از کابل سریال و مبدل، مناسب برای مانیتورینگ محلی و تک‌کاربره.

·         **شبکه محلی و اینترنت :(Network)** اتصال از طریق شبکه‌های محلی یا اینترنت جهت مانیتورینگ چندکاربره و از راه دور.

##### **6.9.1.1.2. وارد کردن آدرس پروژه**

·         کاربر می‌تواند **آدرس دقیق پروژه** را به صورت دستی وارد کند.

·         همچنین امکان **انتخاب موقعیت پروژه از طریق نقشه** وجود دارد تا آدرس به‌صورت دقیق و خودکار ثبت شود.

**اعتبارسنجی:**

آدرس نباید خالی باشد. اگر خالی بود، پیام خطای «وارد کردن آدرس پروژه الزامی است» نمایش داده شود.

##### **6.9.1.1.3. تنظیم ارتباط سریال (Serial Port)**

·         از **منوی بازشونده نوع ارتباط (Communication Type)** گزینه **پورت سریال (Serial Port)** را انتخاب کنید.

·         در قسمت مربوط به پورت سریال، پورت مورد نظر (مثلاً COM4) را از فهرست پورت‌های موجود انتخاب نمایید.

**اعتبارسنجی:**

اگر پورت انتخاب نشد، پیام خطا: «لطفاً یک پورت سریال معتبر انتخاب کنید» نمایش داده شود.

##### **6.9.1.1.4. تنظیم ارتباط شبکه (Network Port)**

·         از **منوی بازشونده نوع ارتباط (Communication Type)** گزینه **پورت شبکه (Network Port)** را انتخاب کنید.

·         در بخش تنظیمات شبکه، **IP  سرور** و **شماره پورت (Port)** را وارد نمایید.

**اعتبارسنجی:**

* IP باید در قالب صحیح IPv4 یا IPv6 باشد.  
* شماره پورت باید عددی بین 1 تا 65535 باشد.  
* در صورت خطا، پیام مناسب مثل «آدرس IP نامعتبر است» یا «شماره پورت باید عدد بین 1 تا 65535 باشد» نشان داده شود.

 

##### **6.9.1.1.5. اعمال تنظیمات**

این دکمه در حالت غیرفعال (disabled) باقی می‌ماند تا زمانی که همه فیلدهای الزامی به درستی پر شده باشند.

پس از کلیک:

* اگر همه داده‌ها معتبر باشند، پیغام موفقیت «تنظیمات با موفقیت ذخیره شد» نمایش داده می‌شود.  
* اگر مشکلی در اتصال وجود داشته باشد، پیغام خطای ارتباط نمایش داده می‌شود.

 

 

 

 

#### **6.9.1.2 مانیتورینگ وضعیت اتصال (Connection Monitoring)**

پس از انتخاب نوع اتصال در منوی **پروتکل ارتباطی** و وارد کردن اطلاعات مربوطه، کاربر می‌تواند با کلیک بر روی **وضعیت اتصال** فرآیند برقراری ارتباط بین نرم‌افزار و پنل اعلام حریق را مدیریت و پایش کند.

دسترسی به وضعیت اتصال از دو مسیر در نرم‌افزار امکان‌پذیر است:

·         از طریق زیرمنوی **« وضعیت اتصال»** در بخش **مدیریت ارتباطات**

·         از طریق نشانگر وضعیت اتصال در **سمت راست نوار مانیتورینگ** که به‌صورت دائمی (درتمامی صفحات نرم افزار) نمایش داده می‌شود و قابل کلیک است

در اولین استفاده، کاربر با کلیک بر روی وضعیت اتصال، فرآیند **برقراری ارتباط اولیه** را آغاز می‌کند و در صورت صحت تنظیمات، وضعیت به حالت **Connected (متصل)** تغییر می‌یابد.

پس از برقراری اتصال، نرم‌افزار به‌صورت **خودکار و Real-Time** وضعیت ارتباط را پایش می‌کند. در صورتی که در حین کار با نرم‌افزار، به هر دلیل ارتباط با پنل قطع شود:

·         وضعیت اتصال به‌صورت خودکار به **Disconnected (قطع)** تغییر می‌کند

·         این تغییر وضعیت بدون نیاز به اقدام کاربر نمایش داده می‌شود تا کاربر فوراً از قطع ارتباط مطلع شود

چنانچه کاربر در هر یک از صفحات نرم‌افزار در حال کار باشد و قطع ارتباط رخ دهد، سیستم موظف است یک **پنجره هشدار (Popup Notification)** نمایش دهد که شامل پیام زیر است:

**« ارتباط با پنل اعلام حریق قطع شده است »**

این پنجره باید دارای دو دکمه عملیاتی باشد:

·         **«اتصال مجدد» (Reconnect)**: برای تلاش مجدد جهت برقراری ارتباط با استفاده از تنظیمات فعلی

·         **«ادامه بدون اتصال»**: برای بستن هشدار و ادامه کار در حالت قطع ارتباط

وضعیت اتصال همواره باید بیانگر **آخرین وضعیت واقعی ارتباط** باشد و در تمام صفحات نرم‌افزار، بدون وابستگی به منوی خاص یا صفحه تنظیمات، قابل مشاهده باقی بماند.

##### **6.9.1.2.1. نمایش وضعیت اتصال (Connection Status)**

وضعیت ارتباط بین نرم‌افزار و پنل اعلام حریق باید به‌صورت **Real-Time**  نمایش داده شود و شامل موارد زیر باشد:

**حالات اصلی:**

·         **Connected (متصل)**

o        نرم‌افزار ارتباط فعال و پایدار با پنل دارد.

·         **Disconnected (قطع)**

o        ارتباط نرم‌افزار با پنل برقرار نیست یا قطع شده است.

##### **6.9.1.2.2. نحوه نمایش بصری (UI Design)**

·         استفاده از **آیکن استاندارد ارتباط** (مانند کابل، لینک یا آنتن)

·         رنگ‌بندی پیشنهادی:

o        سبز → وضعیت متصل

o        قرمز → وضعیت قطع

·         در کنار آیکن، متن وضعیت به‌صورت واضح نمایش داده شود:

o        `Status: Connected`

o        `Status: Disconnected`

##### **6.9.1.2.3. نمایش جزئیات تکمیلی (Tooltip / Hover)**

در صورت قرار گرفتن ماوس روی آیکن وضعیت (در قسمت راست منو مانیتورینگ)، اطلاعات تکمیلی به‌صورت **Tooltip** نمایش داده شود، شامل:

·         نوع پروتکل ارتباطی فعال

·         پورت یا آدرس اتصال

·         آخرین زمان دریافت داده از پنل

·         مدت زمان اتصال فعال (Session Time)

##### **6.9.1.2.4. رفتار سیستم در زمان قطع ارتباط**

**در صورت قطع ارتباط:**

·         تغییر خودکار رنگ آیکن به قرمز

·         نمایش پیام هشدار کوتاه (Non-blocking Alert)

·         ثبت رویداد قطع ارتباط در **Event Log**

·         امکان تلاش مجدد برای اتصال (Reconnect) به‌صورت:

o        خودکار

o        یا با کلیک کاربر روی آیکن وضعیت

##### **6.9.1.2.5. به‌روزرسانی وضعیت**

·         وضعیت اتصال باید:

o        به‌صورت خودکار

o        بدون نیاز به Refresh دستی

o        و با حداقل تأخیر زمانی به‌روزرسانی شود.

 

 

 

#### **6.9.1.3 همگام‌سازی اطلاعات (Data Synchronization)**

به‌منظور اطمینان از یکسان بودن اطلاعات بین نرم‌افزار و پنل اعلام حریق، ماژول **همگام‌سازی اطلاعات** در نرم‌افزار طراحی شده است.  
 این ماژول امکان بررسی، ارسال و دریافت داده‌ها را فراهم می‌کند تا از بروز اختلاف اطلاعات و اعمال ناخواسته تغییرات جلوگیری شود.

دسترسی به این بخش از طریق زیرمنوی **«همگام‌سازی اطلاعات»** در منوی **مدیریت ارتباطات** انجام می‌شود.

##### **6.9.1.3.1. منطق کلی عملکرد** (Overall Functional Logic)

با انتخاب گزینه **همگام‌سازی اطلاعات**، نرم‌افزار ابتدا باید به‌صورت خودکار یک **بررسی اولیه (Pre-Sync Check)** انجام دهد تا مشخص شود آیا بین اطلاعات موجود در نرم‌افزار و پنل اختلافی وجود دارد یا خیر.

بر اساس نتیجه این بررسی، رفتار سیستم به یکی از دو حالت زیر تقسیم می‌شود:

###### **6.9.1.3.1.1. حالت اول: عدم وجود اختلاف اطلاعات (No Data Conflict)**

در صورتی که نرم‌افزار تشخیص دهد اطلاعات پنل و نرم‌افزار کاملاً یکسان هستند، یک پاپاپ اطلاع‌رسانی نمایش داده می‌شود.

**پاپاپ اطلاع‌رسانی همگام بودن اطلاعات**

**مشخصات ظاهری:**

·         عنوان پاپاپ: **همگام‌سازی اطلاعات**

·         دکمه بستن (✕) در گوشه بالای سمت چپ

**متن پیام:**

«اطلاعات موجود در نرم‌افزار و پنل اعلام حریق کاملاً همگام هستند و نیازی به انجام عملیات همگام‌سازی وجود ندارد»

**دکمه‌ها:**

·         فقط یک دکمه:

o        **«متوجه شدم»**

**رفتار سیستم:**

·         با کلیک روی «متوجه شدم»، پاپاپ بسته می‌شود.

·         هیچ فرآیند همگام‌سازی اجرا نمی‌شود.

·         هیچ تغییری در اطلاعات اعمال نمی‌گردد.

در این حالت **هیچ دکمه‌ای برای ارسال یا دریافت اطلاعات نمایش داده نمی‌شود**.

 

###### **6.9.1.3.1.2. حالت دوم: وجود اختلاف اطلاعات (Data Conflict Detected)**

در صورتی که اختلافی بین اطلاعات نرم‌افزار و پنل شناسایی شود، نرم‌افزار باید پاپاپ انتخاب نوع همگام‌سازی را نمایش دهد تا کاربر منبع اصلی داده را مشخص کند.

**پاپاپ انتخاب نوع همگام‌سازی (Sync Direction Selection)**

**مشخصات ظاهری:**

·         عنوان پاپاپ: **همگام‌سازی اطلاعات**

·         دکمه بستن (✕) در گوشه بالای سمت چپ

**متن پیام:**

«بین اطلاعات موجود در نرم‌افزار و پنل اعلام حریق اختلاف‌هایی شناسایی شده است.  
 لطفاً مشخص کنید کدام اطلاعات به‌عنوان مرجع اصلی در نظر گرفته شود»

**دکمه‌های عملیاتی:**

1\.      **دریافت اطلاعات از پنل**  
 *( اطلاعات پنل جایگزین اطلاعات فعلی نرم‌افزار می‌شود)*

2\.      **ارسال اطلاعات به پنل**  
 *(اطلاعات فعلی نرم‌افزار روی پنل اعمال می‌شود)*

**پاپاپ تأیید عملیات (Confirmation Dialog)**

پس از انتخاب هر یک از گزینه‌های فوق، پاپاپ انتخاب بسته شده و پنجره تأیید نمایش داده می‌شود.

**اگر «دریافت اطلاعات از پنل» انتخاب شود:**

**متن پیام:**

«با انجام این عملیات، اطلاعات فعلی نرم‌افزار حذف و با داده‌های موجود در پنل جایگزین خواهد شد.  
 آیا از ادامه این فرآیند اطمینان دارید؟»

**اگر «ارسال اطلاعات به پنل» انتخاب شود:**

**متن پیام:**

«با انجام این عملیات، اطلاعات فعلی پنل با داده‌های موجود در نرم‌افزار بازنویسی می‌شود.  
 آیا مایل به ادامه هستید؟»

**دکمه‌ها:**

·         **« بله، ادامه بده»**

·         **«خیر»**

**رفتار سیستم در زمان اجرای همگام‌سازی**

در صورت انتخاب «بله، ادامه بده»:

·         فرآیند همگام‌سازی آغاز می‌شود.

·         تمام صفحه به حالت **مات (Disabled Overlay)** درمی‌آید.

·         تعامل کاربر با سایر بخش‌ها غیرفعال می‌شود.

·         یک **نوار پیشرفت (Progress Bar)** در مرکز صفحه نمایش داده می‌شود که میزان پیشرفت عملیات را نشان می‌دهد.

در صورت انتخاب «خیر»:

·         عملیات لغو می‌شود.

·         هیچ تغییری در اطلاعات اعمال نمی‌گردد.

##### **6.9.1.3.2. محل قرارگیری در رابط کاربری**

·         زیرمنوی مستقل در **مدیریت ارتباطات**

·         دسترسی سریع از طریق:

o        دکمه اختصاصی

o        یا آیکن همگام‌سازی در Toolbar

·         وضعیت عملیات بدون خروج از صفحه جاری قابل مشاهده باشد.

##### **6.9.1.3.3. انواع همگام‌سازی**

###### **همگام‌سازی خودکار (Automatic Sync)**

·         اجرا به‌صورت خودکار:

o        هنگام اتصال موفق به پنل

o        پس از تغییر تنظیمات

o        در بازه‌های زمانی مشخص

·         بدون دخالت کاربر

·         قابل فعال/غیرفعال شدن از تنظیمات

###### **همگام‌سازی دستی (Manual Sync)**

·         فعال‌سازی توسط کاربر

·         مناسب برای اختلاف داده یا تغییرات حساس

##### **6.9.1.3.4. داده‌های قابل همگام‌سازی**

·         تنظیمات پنل

·         اطلاعات تجهیزات

·         نام‌گذاری زون‌ها، لوپ‌ها و تجهیزات

·         وضعیت‌ها، خطاها و هشدارها

·         تاریخچه رویدادها (در صورت فعال بودن)

##### **6.9.1.3.5. ثبت گزارش همگام‌سازی (Synchronization Log)**

به‌منظور امکان پیگیری، عیب‌یابی و بررسی سوابق عملکرد سیستم، کلیه عملیات مربوط به همگام‌سازی اطلاعات باید به‌صورت خودکار در **گزارش‌ها (Logs)** ذخیره شود.

برای هر فرآیند همگام‌سازی، اطلاعات زیر در گزارش ثبت می‌گردد:

·         **تاریخ و زمان** انجام عملیات

·         **نوع عملیات** (دریافت اطلاعات از پنل، ارسال اطلاعات به پنل، همگام‌سازی خودکار یا دستی)

·         **نتیجه نهایی عملیات** (موفق یا ناموفق)

·         **کاربر انجام‌دهنده عملیات** (در صورت وجود سطح دسترسی و ورود کاربر)

این گزارش‌ها باید در بخش گزارش‌های نرم‌افزار قابل مشاهده بوده و برای **بررسی سوابق، تحلیل خطاها و کنترل تغییرات اعمال‌شده** مورد استفاده قرار گیرند.

 

 

 

 

 

 

 

 

 

 

 

#### **6.9.1.2. نمایش گرافیکی سیستم  (Visualization)**

*(محیط بصری برای مدیریت نقشه ساختمان و موقعیت تجهیزات اعلام حریق)*

این بخش به منظور نمایش بصری ساختمان، نقشه‌ها و جانمایی تجهیزات اعلام حریق طراحی شده است و به‌عنوان محیط اصلی کار با نقشه‌ها و نمایش گرافیکی سیستم عمل می‌کند.

این قسمت در منوی اصلی مانیتورینگ قرار دارد و داری زیر منو هایی برای تنظیم است که شامل :

·         **بارگذاری نقشه:** آپلود نقشه دو بعدی یا سه بعدی  
·         **جانمایی تجهیزات:** جا نمایی دیوایس ها بر روی نقشه  
·         **مانیتورینگ وضعیت:** نمایش لحظه ای وضعیت تجهیزات روی نقشه

##### **6.9.1.2.1. بارگذاری نقشه یا ورود اولیه به بخش نمایش گرافیکی**

در صورتی که کاربر برای **اولین بار** وارد بخش نمایش گرافیکی شود و هیچ نقشه‌ای از قبل بارگذاری نشده باشد، نرم‌افزار به‌صورت خودکار صفحه **بارگذاری نقشه** را نمایش می‌دهد.

در این صفحه، توضیح راهنمای زیر به کاربر نمایش داده می‌شود:

«برای شروع، نقشه ساختمان خود را بارگذاری کنید.  
 می‌توانید نقشه را به‌صورت دو بعدی یا سه بعدی انتخاب و وارد نرم‌افزار نمایید.»

در این صفحه دو دکمه وجود دارد:

·         **بارگذاری نقشه دو بعدی**

·         **بارگذاری نقشه سه بعدی**

با انتخاب هر یک از این دکمه‌ها، پنجره انتخاب فایل باز شده و کاربر می‌تواند مسیر فایل نقشه ساختمان را انتخاب کند.

##### **فرمت‌های قابل پشتیبانی نقشه**

·         فرمت‌های مجاز: **DXF**  و **DWG**

·         پس از انتخاب و بارگذاری موفق نقشه، صفحه بارگذاری اولیه حذف شده و وارد مرحله بعد می‌شود.

##### **ثبت عنوان نقشه**

**پس از بارگذاری نقشه:**

·         تصویر نقشه به‌صورت **بندانگشتی (Thumbnail)** نمایش داده می‌شود.

·         در زیر تصویر، بخشی با عنوان **« عنوان نقشه»** نمایش داده می‌شود.

·         کاربر باید یک عنوان برای نقشه وارد کند.

##### **دکمه تأیید عنوان:**

·         تا زمانی که عنوان وارد نشده باشد، دکمه **« تأیید»** غیرفعال است.

·         پس از وارد کردن عنوان، دکمه فعال می‌شود.

·         با کلیک روی: **« تأیید»**

o        عنوان نقشه ذخیره می‌شود

o        دکمه تأیید حذف می‌گردد

o        دکمه **« تنظیمات نقشه»** جایگزین آن می‌شود

##### **دکمه حذف نقشه**

·         در کنار هر بندانگشتی نقشه، دکمه **«حذف نقشه»** قرار دارد.

·         با کلیک روی این دکمه، پنجره پاپ‌آپ تأیید حذف باز می‌شود.

###### **پاپ‌آپ تأیید حذف نقشه**

**متن پاپ‌آپ:**

« آیا از حذف این نقشه اطمینان دارید؟  
 توجه داشته باشید با حذف نقشه، اطلاعات جانمایی تجهیزات مربوط به این نقشه نیز حذف خواهد شد.»

**دکمه‌ها:**

·         **«بله، حذف کن»**

o        نقشه به همراه اطلاعات مرتبط حذف می‌شود.

o        رابط کاربری به‌روزرسانی شده و نقشه حذف‌شده دیگر نمایش داده نمی‌شود.

·         **«خیر، منصرف شدم»**

o        پاپ‌آپ بسته می‌شود.

o        هیچ تغییری اعمال نمی‌شود.

##### **دسترسی به تنظیمات نقشه و جانمایی تجهیزات**

پس از ذخیره عنوان نقشه، کاربر می‌تواند از سه روش وارد محیط تنظیمات و جانمایی تجهیزات شود:

1\.      از طریق زیرمنوی **نمایش گرافیکی** و انتخاب گزینه  
 **«جانمایی تجهیزات روی نقشه»**

2\.      با کلیک روی دکمه **«تنظیمات نقشه»**

3\.      با کلیک روی تصویر بندانگشتی نقشه

محیط تنظیمات نقشه، محل اصلی **افزودن، جابه‌جایی و مدیریت موقعیت تجهیزات اعلام حریق** روی نقشه است.

##### **افزودن نقشه‌های بیشتر**

**پس از بارگذاری اولین نقشه:**

·         پس از بارگذاری اولین نقشه، در کنار **بندانگشتی نقشه‌های قبلی**، علامت **«+»**  نمایش داده می‌شود.

·         این علامت با توضیح کوتاه **«برای افزودن نقشه جدید، اینجا کلیک کنید»** همراه است تا کاربر به‌راحتی متوجه عملکرد آن شود.

·         با کلیک روی علامت «+»، کاربر وارد صفحه انتخاب فایل می‌شود تا نقشه جدید را بارگذاری کند.

·         این امکان به کاربر اجازه می‌دهد نقشه‌های متعدد ساختمان را به نرم‌افزار اضافه کند و مدیریت بهتری روی بخش‌های مختلف ساختمان داشته باشد.

**قوانین بارگذاری نقشه‌های بعدی:**

·         اگر اولین نقشه به‌صورت **دو بعدی** بارگذاری شده باشد، تمام نقشه‌های بعدی نیز باید دو بعدی باشند.

·         اگر اولین نقشه **سه بعدی** باشد، سایر نقشه‌ها نیز باید سه بعدی انتخاب شوند.

·         امکان ترکیب نقشه‌های دو بعدی و سه بعدی در یک پروژه وجود ندارد.

##### **تعداد نقشه‌ها**

·         محدودیتی در تعداد نقشه‌های قابل بارگذاری وجود ندارد.

·         کاربر می‌تواند متناسب با ساختار ساختمان (طبقات، بخش‌ها، زون‌ها) هر تعداد نقشه مورد نیاز را اضافه کند.

##### **نکات طراحی حرفه‌ای (Design Considerations \-پیشنهادی)**

·         نمایش واضح تفاوت نقشه فعال با سایر نقشه‌ها (Highlight)

·         جلوگیری از حذف یا جایگزینی نقشه بدون تأیید کاربر

·         امکان نمایش نام نقشه هنگام قرار گرفتن موس روی بندانگشتی

·         حفظ عملکرد روان نرم‌افزار در صورت وجود تعداد زیاد نقشه

·         طراحی ساده و قابل فهم برای استفاده در شرایط اضطراری

 

 

 

 

 

 

 

 

##### **6.9.1.2.2. جانمایی تجهیزات (Device Placement)**

###### **ورود به صفحه جانمایی تجهیزات**

پس از بارگذاری نقشه، صفحه جانمایی تجهیزات برای کاربر فعال می‌شود. کاربر می‌تواند به دو روش به این صفحه منتقل شود:

·         از صفحه بارگذاری نقشه با انتخاب دکمه **«تنظیمات»** زیر تصویر بندانگشتی نقشه

·         از طریق زیرمنوی **نمایش گرافیکی سیستم** و انتخاب گزینه **«جانمایی تجهیزات»**

###### **ساختار صفحه جانمایی تجهیزات**

صفحه جانمایی تجهیزات به دو بخش اصلی تقسیم می‌شود:

·         **سمت چپ:**  
 نمایش نقشه‌های دو بعدی و سه بعدی ساختمان به همراه تجهیزات اعلام حریق (دتکتورها، شستی‌ها، آژیرها و سایر دستگاه‌ها) جانمایی شده روی نقشه.

·         **سمت راست:**  
 منوی تنظیمات و گزینه‌های مدیریتی برای ویرایش، افزودن، حذف و مدیریت کامل تجهیزات روی نقشه.

**ویژگی‌ها و طراحی بخش نقشه (سمت چپ)**

·         **بزرگ‌نمایی و کوچک‌نمایی:**

o        امکان استفاده از اسکرول موس برای بزرگ و کوچک کردن نقشه

o        دو دکمه ثابت با آیکن‌های «+» و «−» در گوشه بالا سمت چپ برای بزرگ‌نمایی و کوچک‌نمایی نقشه

·         **جابجایی نقشه:**

o        نگه داشتن کلید **Space** و کلیک چپ موس همراه با حرکت موس برای جابجایی آزاد نقشه

o        اسکرول‌های عمودی و افقی در اطراف نقشه برای جابجایی در جهت‌های مختلف

o        دکمه **آیکن دست** زیر دکمه‌های بزرگنمایی که با فعال کردن آن، موس به شکل دست درآمده و با کلیک و کشیدن نقشه جابجا می‌شود

**·**         **Undo/Redo :**

o        دکمه **Undo** (یا کلید میانبر **Ctrl \+ Z**) برای بازگرداندن آخرین تغییرات و ویرایش‌های انجام شده روی نقشه در نظر گرفته شده است.

o        دکمه **Redo** (یا کلید میانبر **Ctrl \+ Y**) امکان بازگشت به حالت قبل از انجام Undo را فراهم می‌کند و به کاربر اجازه می‌دهد تغییرات بازگردانده شده را دوباره اعمال کند.

·         **جستجوی تجهیزات:**

o        کادر جستجو در گوشه بالا سمت راست نقشه

o        امکان جستجو بر اساس:

§         نوع تجهیزات (دتکتور دود، حرارت، شستی و ...)

§         نام دلخواه تجهیزات

§         شماره دستگاه یا لوپ

o        نتایج جستجو به صورت هایلایت دور تجهیزات نمایش داده می‌شود (در صورت وجود چند دستگاه، همه آنها نشان داده می‌شوند)

·         **نمایش اطلاعات تکمیلی:**

o        هنگام قرارگیری موس روی تجهیزات نقشه، یک tooltip نمایش داده می‌شود که شامل اطلاعات زیر است:

§         نام دستگاه

§         شماره لوپ

§         شماره دیوایس

§         نوع دستگاه

###### **منوی تنظیمات سمت راست با قابلیت پین  (Pin)**

این منو دارای قابلیت **تغییر اندازه (Resize)** است. به‌گونه‌ای که با قرار گرفتن موس روی گوشه یا لبه منوی تنظیمات، شکل نشانگر موس تغییر کرده و کاربر می‌تواند با کلیک و کشیدن، اندازه منو را به‌صورت دلخواه بزرگ یا کوچک کند..

·         **آیکن پین:**

o        در گوشه بالا سمت چپ منوی تنظیمات، آیکنی به شکل سوزن یا پین قرار دارد که وضعیت پین بودن منو را نشان می‌دهد

o        ظاهر آیکن با توجه به وضعیت پین تغییر می‌کند (رنگ، زاویه یا جلوه بصری دیگر)

·         **حالت‌های نمایش منو:**

1\.	**حالت پین شده (Pinned):**

·         منو همیشه باز و قابل مشاهده است

·         منو حتی در صورت حرکت موس خارج از منو نیز بسته نمی‌شود

2\.    **حالت شناور (Unpinned):**

·         منو به صورت جمع‌شده یا مخفی است و تنها عنوان **«تنظیمات نقشه»** به صورت عمودی در سمت راست صفحه نمایش داده می‌شود.

·         زمانی که موس روی عنوان **«تنظیمات نقشه»** قرار گیرد، منوی تنظیمات به صورت انیمیشنی و روان باز می‌شود (Slide-in).

·         با خروج موس از ناحیه منو، منو به صورت انیمیشنی و نرم بسته می‌شود (Slide-out).

·         **تعامل با آیکن پین:**

o        کلیک روی آیکن پین بین دو حالت پین شده و شناور جابجا می‌شود

o        تغییر وضعیت با انیمیشن نرم و روان انجام شده و تجربه کاربری بهینه را تضمین می‌کند

o        وضعیت کنونی با آیکن و tooltip قابل مشاهده است

·         **توجه‌های فنی:**

o        منو باید **واکنش‌گرا (Responsive)** بوده و روی دستگاه‌های مختلف (کامپیوتر، تبلت، موبایل) به خوبی عمل کند

o        وضعیت پین شدن منو باید در تنظیمات کاربر ذخیره و در ورودهای بعدی بازیابی شود

o        انیمیشن‌ها و تعاملات موس باید برای سرعت‌ها و دستگاه‌های مختلف بهینه باشند

 

 

###### **نحوه جانمایی تجهیزات روی نقشه**

در سمت راست صفحه، بخش **تنظیمات نقشه** قرار دارد که فرآیند جانمایی تجهیزات از طریق آن انجام می‌شود. این بخش شامل مراحل و قابلیت‌های زیر است:

**۱**. **خواندن اطلاعات تجهیزات از کارت لوپ**

در حالت اولیه، بخش تنظیمات نقشه هیچ لیستی از تجهیزات نمایش نمی‌دهد و تنها یک پیام راهنما به کاربر نمایش داده می‌شود:

«برای جانمایی تجهیزات روی نقشه، ابتدا روی دکمه *خواندن دیوایس‌ها* کلیک کنید»

با کلیک کاربر روی دکمه **«خواندن دیوایس‌ها»**، سیستم به پنل اعلام حریق متصل شده و کلیه تجهیزات تعریف‌شده روی کارت‌های لوپ را از پنل خوانده شود.

**خواندن اطلاعات دیوایس‌ها از پنل**

با کلیک کاربر روی دکمه **«خواندن دیوایس‌ها»**، سیستم به پنل اعلام حریق متصل شده و وضعیت تجهیزات را بررسی می‌کند.

در این مرحله، سیستم ابتدا بررسی می‌کند که آیا دیوایس‌ها قبلاً **آدرس‌دهی و تنظیمات آن‌ها ذخیره شده است یا خیر**:

·         **در صورتی که تجهیزات آدرس‌دهی نشده باشند**، یک پنجره پاپ‌آپ هشدار به کاربر نمایش داده می‌شود با متن زیر:

«لطفاً ابتدا با استفاده از نرم‌افزار پنل یا از طریق خود پنل، دیوایس‌ها را آدرس‌دهی کرده و تنظیمات را ذخیره کنید. سپس برای جانمایی تجهیزات روی نقشه اقدام نمایید»

تا زمان انجام آدرس‌دهی صحیح و ذخیره تنظیمات، **امکان ادامه فرآیند جانمایی تجهیزات وجود نخواهد داشت**.

·         **در صورتی که تجهیزات به‌درستی آدرس‌دهی شده باشند**، سیستم اطلاعات کلیه دیوایس‌ها را از پنل خوانده و وارد مرحله بعد می‌شود.

**انتخاب کارت لوپ و نمایش اطلاعات آماری**

پس از خواندن موفق اطلاعات از پنل، در بالای بخش تنظیمات نقشه یک **لیست کشویی انتخاب کارت لوپ** نمایش داده می‌شود.

با انتخاب هر کارت لوپ، در بالای لیست تجهیزات اطلاعات آماری مربوط به همان کارت لوپ نمایش داده می‌شود که شامل موارد زیر است:

·         تعداد کل دیوایس‌های موجود در کارت لوپ

·         تعداد هر نوع دیوایس (مانند دتکتور دود، دتکتور حرارت، شستی و سایر تجهیزات)

این اطلاعات به کاربر کمک می‌کند تا درک سریع و دقیقی از وضعیت کلی تجهیزات کارت لوپ انتخابی داشته باشد.

**بارگذاری لیست تجهیزات کارت لوپ**

پس از نمایش اطلاعات آماری، تجهیزات مربوط به کارت لوپ انتخاب‌شده در بخش پایین تنظیمات بارگذاری و نمایش داده می‌شوند.

در این بخش، دو دکمه برای انتخاب نوع نمایش تجهیزات در اختیار کاربر قرار می‌گیرد:

·         **نمایش درختی**

·         **نمایش لیستی**

**نمایش درختی تجهیزات** (Tree View)

در حالت نمایش درختی، تمامی تجهیزات کارت لوپ انتخاب‌شده به‌صورت ساختار درختی نمایش داده می‌شوند و دیوایس‌ها به ترتیب شماره، از **۱ تا ۲۵۴** مرتب شده‌اند.

این حالت برای بررسی ساختار کلی لوپ و مشاهده ترتیب دیوایس‌ها مناسب است.

**نمایش لیستی تجهیزات** (List View)

در حالت نمایش لیستی، تجهیزات کارت لوپ در قالب یک جدول نمایش داده می‌شوند که اطلاعات کامل هر دیوایس را شامل می‌شود، از جمله:

·         شماره دیوایس

·         نوع دیوایس

·         نام دیوایس

·         آدرس دیوایس

·         وضعیت دیوایس (فعال / غیرفعال)

در صورتی که عرض بخش تنظیمات محدود باشد، **اسکرول عمودی** و **اسکرول افقی** در پایین جدول فعال می‌شود تا امکان مشاهده کامل اطلاعات هر دیوایس فراهم شود.

**قابلیت جستجوی تجهیزات**

در بالای لیست تجهیزات، قابلیت **جستجو** در نظر گرفته شده است که به کاربر امکان می‌دهد تجهیزات را بر اساس موارد زیر فیلتر کند:

·         نوع دیوایس

·         شماره دیوایس

پس از انجام جستجو، **فقط دیوایس‌هایی که با معیار جستجو مطابقت دارند نمایش داده می‌شوند** و سایر تجهیزات به‌صورت موقت از لیست مخفی خواهند شد.

 

 

 

 

 

#### **افزودن دیوایس به نقشه**

## **افزودن دیوایس به نقشه (Device Placement on Map)**

فرآیند افزودن دیوایس به نقشه به سه روش انجام می‌شود:

1\.      افزودن از طریق **نمایش درختی**

2\.      افزودن از طریق **نمایش لیستی**

3\.  	افزودن مستقیم از روی **خود نقشه**

## **۱. افزودن دیوایس از طریق نمایش درختی (Tree View)**

### **ظاهر دیوایس‌ها در حالت درختی**

در حالت نمایش درختی، هر دیوایس به صورت زیر نمایش داده می‌شود:

·         ابتدا **شماره دیوایس**

·         سپس **آیکن یا شماتیک دیوایس**

·         و در ادامه **عنوان دیوایس** (مثلاً دتکتور دود)

### **نحوه افزودن دیوایس به نقشه (Drag & Drop)**

·         کاربر می‌تواند با **کلیک چپ موس روی آیکن یا نام دیوایس** و نگه داشتن آن، دیوایس را بگیرد.

·         سپس با کشیدن موس، دیوایس را به محل دلخواه روی نقشه (سمت چپ صفحه) منتقل کند.

·         با رها کردن کلیک موس، دیوایس در آن موقعیت روی نقشه قرار می‌گیرد.

### **رفتار دیوایس پس از جانمایی**

پس از قرار گرفتن دیوایس روی نقشه:

·         دیوایس مربوطه در لیست درختی **کم‌رنگ و غیرفعال** می‌شود.

·         این حالت نشان‌دهنده این است که دیوایس قبلاً روی نقشه جانمایی شده است.

·         در مقابل همان دیوایس در لیست، **آیکن قفل باز** نمایش داده می‌شود.

·         در مقابل همان دیوایس علاوه بر آیکن قفل باز، دکمه حذف هم نمایش داده می شود.

### **قابلیت قفل‌کردن دیوایس روی نقشه**

·         **قفل باز:**

o        دیوایس روی نقشه قابل جابجایی است.

o        کاربر می‌تواند با کلیک و کشیدن موس، موقعیت آن را تغییر دهد.

·         **قفل بسته:**

o        با کلیک روی آیکن قفل باز، وضعیت به قفل بسته تغییر می‌کند.

o        در این حالت، دیوایس روی نقشه ثابت شده و امکان جابجایی ندارد.

o        برای جابجایی مجدد، کاربر باید دوباره قفل را باز کند.

### **حذف دیوایس از نقشه**

·         پس از جانمایی دیوایس، **دکمه حذف** در کنار آیکن و نام دیوایس نمایش داده می‌شود.

·         با کلیک روی دکمه حذف:

o        دیوایس از روی نقشه حذف می‌شود.

o        دیوایس مربوطه در لیست درختی از حالت کم‌رنگ خارج شده و دوباره فعال می‌شود.

o        امکان جانمایی مجدد دیوایس در محل جدید فراهم می‌شود.

## **۲. افزودن دیوایس از طریق نمایش لیستی (List View)**

### **ظاهر دیوایس‌ها در حالت لیستی**

در حالت نمایش لیستی، دیوایس‌ها در قالب یک جدول نمایش داده می‌شوند که شامل ستون‌های زیر است:

·         شماره دیوایس

·         نوع دیوایس

·         آیکن یا شماتیک دیوایس

·         نام دیوایس

·         آدرس دیوایس

·         وضعیت دیوایس (فعال / غیرفعال)

در صورتی که اطلاعات جدول زیادتر از اندازه جدول باشد با استفاده از اسکرول عمودی و افقی می توان تمام اطلاعت دیوایس ها را مشاهده کرد.

### **نحوه افزودن دیوایس به نقشه**

·         کاربر می‌تواند **از هر بخش از ردیف جدول** با کلیک چپ موس و نگه داشتن آن، دیوایس را بگیرد.

·         دیوایس را به محل مورد نظر روی نقشه منتقل کرده و کلیک موس را رها کند.

·         با این کار، شماتیک دیوایس روی نقشه قرار می‌گیرد.

### **رفتار دیوایس پس از جانمایی**

·         ردیف مربوط به دیوایس در جدول **کم‌رنگ** می‌شود.

·         در کنار ردیف، **آیکن قفل باز** و **دکمه‌ی حذف** نمایش داده می‌شود.

·         این وضعیت نشان می‌دهد که دیوایس روی نقشه قرار گرفته، قابل جابجایی است و می توان آن دیواس را از روی نقشه حذف کرد.

### **قفل‌کردن و حذف دیوایس**

·         با کلیک روی آیکن قفل باز، وضعیت به **قفل بسته** تغییر کرده و دیوایس روی نقشه ثابت می‌شود.

·         با کلیک روی دکمه **حذف**:

o        دیوایس از نقشه حذف می‌شود.

o        ردیف مربوطه در جدول دوباره فعال شده و امکان جانمایی مجدد فراهم می‌شود.

## **۳. افزودن دیوایس به‌صورت مستقیم از روی نقشه**

در این روش، مستقل از اینکه نمایش درختی یا نمایش لیستی فعال باشد، امکان استفاده از این قابلیت وجود دارد؛ با این شرط که کاربر حتماً یکی از این دو حالت نمایش را انتخاب کرده باشد. در این حالت، کاربر می‌تواند دیوایس را مستقیماً از طریق نقشه به سیستم اضافه کند.

### **انتخاب ناحیه روی نقشه**

·         کاربر با **کلیک چپ موس، نگه داشتن و کشیدن روی نقشه** و سپس رها کردن موس، یک ناحیه مشخص را انتخاب می‌کند.

·         ناحیه انتخاب‌شده با **رنگ آبی نیمه‌شفاف** نمایش داده می‌شود تا محدوده انتخاب به‌وضوح قابل تشخیص باشد.

·         پس از انتخاب ناحیه، یک **پاپ‌آپ عمودی** نمایش داده می‌شود که شامل لیست انواع دیوایس‌هایی است که درون لوپ انتخاب شده قراردارد.

·         Smoke Detector (دتکتور دود)

·         Heat Detector (دتکتور حرارت)

·         Multi Detector (دتکتور ترکیبی)

·         Manual Call Point (MCP) (شستی)

·         Sounder / Output (آژیر / خروجی)

·         Zone Module (ماژول زون)

·         Input Module (ماژول ورودی)

·         Input/Output Module (ماژول ورودی/خروجی)

### **انتخاب شماره دیوایس**

·         پس از انتخاب نوع دیوایس، پاپ‌آپ اول بسته شده و یک **پاپ‌آپ دوم** باز می‌شود.

·         در این پاپ‌آپ، لیست شماره دیوایس‌های موجود از همان نوع دیوایس نمایش داده می‌شود.

·         کاربر شماره مورد نظر را انتخاب می‌کند.

### **جانمایی نهایی**

·         پس از انتخاب شماره دیوایس:

o        شماتیک دیوایس روی نقشه نمایش داده می‌شود.

o        دیوایس مربوطه در لیست (درختی یا لیستی) کم‌رنگ می‌شود که نشان‌دهنده استفاده شدن آن است.

## **تعامل با دیوایس‌های جانمایی‌شده روی نقشه**

·         با قرار گرفتن موس روی هر دیوایس روی نقشه، یک Tooltip نمایش داده می‌شود که شامل:

o        شماره دیوایس

o        نوع دیوایس

o        آدرس دیوایس

·         با حرکت موس، Tooltip به‌صورت خودکار محو می‌شود.

·         با کلیک روی دیوایس:

o        وضعیت قفل (باز یا بسته) نمایش داده می‌شود.

o        دکمه **حذف دیوایس از نقشه** در دسترس قرار می‌گیرد.

·         با حذف دیوایس:

o        دیوایس از نقشه حذف می‌شود.

o        دیوایس مجدداً به لیست تجهیزات اضافه شده و فعال می‌شود.

## **ذخیره‌سازی دیوایس‌ها**

با هر بار ایجاد تغییر در نقشه، **تغییرات به‌صورت خودکار (Auto Save)** ذخیره می‌شوند.

·         در صورتی که نرم‌افزار به پنل متصل باشد، اطلاعات و تغییرات اعمال‌شده **به‌صورت مستقیم در پنل نیز ذخیره می‌شود**.

·         در صورتی که نرم‌افزار به پنل متصل نباشد، تغییرات **به‌صورت موقت در داخل نرم‌افزار ذخیره می‌شوند**.

·         پس از اتصال مجدد پنل به نرم‌افزار، کاربر می‌تواند با انتخاب دکمه **«همگام‌سازی»**، اطلاعات ذخیره‌شده در نرم‌افزار را به پنل منتقل کند.

این فرآیند تضمین می‌کند که هیچ تغییری از بین نرفته و داده‌ها در زمان مناسب بین نرم‌افزار و پنل هماهنگ شوند.

 

 

 

 

 

 

 

 

 

 

 

 

 

 

## **6.9.3. مدیریت رویدادها (Event Management)**

این بخش به‌منظور **نمایش آنلاین، لحظه‌ای و زنده وضعیت پنل اعلام حریق** طراحی شده است و کلیه وقایع ثبت‌شده در پنل را به‌صورت آنی در اختیار کاربر قرار می‌دهد.  
 کاربر می‌تواند در هر لحظه مشاهده کند که پنل در چه وضعیتی قرار دارد (حریق، خطا یا وضعیت عادی).

### **6.9.3.1. دریافت و نمایش آنلاین رویدادها**

نرم‌افزار تمامی رویدادهای ثبت‌شده در پنل، شامل:

1\.      **حریق (Fire)**

2\.      **خطا (Fault)**

را به‌صورت لحظه‌ای دریافت کرده و آن‌ها را به شکل شماتیکی روی نقشه‌های **دو‌بعدی و سه‌بعدی** نمایش می‌دهد.

هم‌زمان، لیست رویدادها در زیر یا کنار نقشه با جزئیات کامل زیر نمایش داده می‌شود:

·         نوع رویداد (Fire / Fault)

·         مکان وقوع رویداد

·         آدرس دقیق

·         توضیحات

·         تاریخ و ساعت وقوع

همچنین با قرار گرفتن نشانگر موس روی ناحیه‌ای از نقشه که دچار حریق یا خطا شده است، **یک پنجره اطلاعاتی در کنار موس** باز می‌شود که مشخصات همان ناحیه را نمایش می‌دهد.

کاربر می‌تواند این رویدادها را **از چندین سیستم و از هر نقطه جهان از طریق بستر اینترنت** مشاهده و کنترل کند.

#### **6.9.3.2. واکنش نرم‌افزار به رویدادها (مدیریت آلارم‌ها)**

پس از دریافت هر رویداد، نرم‌افزار به‌صورت خودکار و متناسب با نوع رویداد، واکنش لازم را انجام می‌دهد که شامل نمایش بصری، فعال‌سازی هشدار صوتی و نمایش پنجره آلارم است.

##### **6.9.3.2.1. آلارم حریق (Fire Alarm)**

در صورت وقوع حریق، نرم‌افزار به‌صورت خودکار واکنش‌های زیر را انجام می‌دهد:

·         ناحیه یا دیوایس مربوطه بر روی نقشه‌های دو‌بعدی و سه‌بعدی به‌صورت **قرمز چشمک‌زن** نمایش داده می‌شود.

·         **آژیر سیستم** فعال می‌گردد.

·         در صورتی که کاربر در زمان وقوع رویداد در بخش مدیریت رویدادها حضور نداشته باشد، صفحه **مدیریت رویدادها** به‌صورت خودکار باز شده و ناحیه دچار حریق به‌صورت برجسته نمایش داده می‌شود.

با قرار گرفتن نشانگر موس روی ناحیه دارای حریق، پنجره‌ای اطلاعاتی در کنار موس نمایش داده می‌شود که شامل موارد زیر است:

·         نوع رویداد (Fire)

·         مکان وقوع حریق

·         آدرس دقیق

·         توضیحات

·         تاریخ و ساعت وقوع رویداد

همین اطلاعات به‌صورت لیستی در پایین نقشه نیز نمایش داده می‌شود و کاربر به دکمه‌های کنترلی مرتبط با آلارم دسترسی خواهد داشت.

###### **امکانات نقشه در نمایش آلارم حریق**

نقشه نمایش‌داده‌شده در زمان وقوع آلارم حریق دارای قابلیت‌های کنترلی زیر است تا کاربر بتواند محل خطا را با دقت بررسی کند:

·         **جابجایی نقشه (Pan):**

o        نگه داشتن کلید **Space**

o        کلیک چپ موس و کشیدن نقشه

·         **اسکرول خودکار:**

o        در صورت بزرگ‌تر بودن نقشه از ابعاد صفحه، اسکرول‌بارهای **عمودی و افقی** فعال می‌شوند.

·         **ابزار دست (Pan Tool):**

o        در بالای سمت چپ نقشه، آیکنی به‌شکل **دست** قرار دارد.

o        با فعال‌سازی آن، نشانگر موس تغییر کرده و امکان حرکت آزادانه روی نقشه فراهم می‌شود.

·         **بزرگ‌نمایی و کوچک‌نمایی (Zoom):**

o        با استفاده از اسکرول موس

o        از طریق دکمه‌های **\+** و **–** در بالای سمت چپ نقشه

o        با اسکرول زوم در پایین سمت چپ صفحه که میزان بزرگ‌نمایی به‌صورت **درصد عددی** نمایش داده می‌شود

###### **مدیریت آلارم حریق در چند نقشه به‌صورت هم‌زمان**

در صورتی که آلارم حریق به‌صورت هم‌زمان در **چند نقشه‌ی مختلف** (مانند چند طبقه یا بخش‌های متفاوت ساختمان) رخ دهد، نرم‌افزار رفتار زیر را اعمال می‌کند:

·         برای **هر نقشه‌ای که حداقل یک آلارم حریق در آن ثبت شده باشد**، یک **تب مجزا** ایجاد می‌شود.

·         عنوان هر تب، **نام نقشه** مطابق با نام تعریف‌شده در تنظیمات سیستم خواهد بود (مانند: همکف، طبقه اول، پارکینگ و …).

·         تمامی تب‌ها در بالای بخش نمایش نقشه قرار می‌گیرند و کاربر می‌تواند به‌سرعت بین آن‌ها جابجا شود.

·         به‌صورت پیش‌فرض، **نقشه‌ای که اولین آلارم حریق در آن شناسایی شده است** (بر اساس ترتیب دریافت رویداد) فعال و نمایش داده می‌شود.

###### **رفتار نرم‌افزار هنگام تغییر تب‌ها**

با انتخاب هر تب توسط کاربر:

·         تنها **نقشه مربوط به همان تب** نمایش داده می‌شود.

·         نواحی دچار حریق در آن نقشه به‌صورت **قرمز چشمک‌زن** مشخص می‌شوند.

·         لیست رویدادهای مربوط به همان نقشه در پایین نقشه بارگذاری می‌گردد.

·         دکمه‌های کنترلی مرتبط با آلارم (قطع آژیر، قطع بازر، کنار گذاشتن) صرفاً بر روی **رویدادهای همان نقشه فعال** اعمال می‌شوند.

###### **دکمه‌های کنترلی آلارم حریق**

کاربر در بخش مدیریت آلارم حریق به گزینه‌های کنترلی زیر دسترسی دارد:

·         **قطع صدای آژیر**  
 با انتخاب این گزینه، صدای آژیر قطع شده و حالت قرمز چشمک‌زن از روی نقشه حذف می‌شود.  
 *(صدای بازر پنل همچنان فعال باقی می‌ماند)*

·         **قطع صدای هشدار پنل (بازر)**  
 این گزینه تنها پس از قطع آژیر فعال می‌شود و امکان قطع صدای بازر پنل را فراهم می‌کند.

·         **کنار گذاشتن**  
 پنجره هشدار بسته می‌شود، اما صدای آژیر همچنان فعال باقی می‌ماند و رویداد در وضعیت فعال حفظ می‌شود.

 

 

 

 

 

 

 

 

##### **6.9.3.2.2 آلارم خطا (Fault Alarm)**

در صورت بروز خطا در سیستم، نرم‌افزار به‌صورت خودکار واکنش‌های زیر را انجام می‌دهد:

·         ناحیه یا دیوایس مربوطه بر روی نقشه‌های دو‌بعدی و سه‌بعدی به‌صورت **زرد چشمک‌زن** نمایش داده می‌شود.

·         **صدای بازر پنل** فعال می‌گردد.

·         در صورتی که کاربر در زمان وقوع رویداد در بخش مدیریت رویدادها حضور نداشته باشد، صفحه **مدیریت رویدادها** به‌صورت خودکار باز شده و ناحیه دچار خطا به‌صورت برجسته نمایش داده می‌شود.

·         در تمامی صفحات نرم‌افزار، در بخش بالایی صفحه، پیامی با **هایلایت زردرنگ** با عنوان  
 **«خطا در پنل اتفاق افتاده است»**  
 به‌صورت سراسری نمایش داده می‌شود.

###### **اطلاعات نمایش‌داده‌شده برای آلارم خطا**

با نمایش آلارم خطا، اطلاعات زیر در پنجره هشدار و همچنین در لیست رویدادهای پایین نقشه نمایش داده می‌شود:

·         نوع خطا

·         مکان وقوع خطا

·         آدرس دقیق دیوایس

·         توضیحات

·         تاریخ و ساعت وقوع رویداد

###### **امکانات نقشه در نمایش آلارم خطا**

نقشه نمایش‌داده‌شده در زمان وقوع آلارم خطا دارای قابلیت‌های کنترلی زیر است تا کاربر بتواند محل خطا را با دقت بررسی کند:

·         **جابجایی نقشه (Pan):**

o        نگه داشتن کلید **Space**

o        کلیک چپ موس و کشیدن نقشه

·         **اسکرول خودکار:**

o        در صورت بزرگ‌تر بودن نقشه از ابعاد صفحه، اسکرول‌بارهای **عمودی و افقی** فعال می‌شوند.

·         **ابزار دست (Pan Tool):**

o        در بالای سمت چپ نقشه، آیکنی به‌شکل **دست** قرار دارد.

o        با فعال‌سازی آن، نشانگر موس تغییر کرده و امکان حرکت آزادانه روی نقشه فراهم می‌شود.

·         **بزرگ‌نمایی و کوچک‌نمایی (Zoom):**

o        با استفاده از اسکرول موس

o        از طریق دکمه‌های **\+** و **–** در بالای سمت چپ نقشه

o        با اسکرول زوم در پایین سمت چپ صفحه که میزان بزرگ‌نمایی به‌صورت **درصد عددی** نمایش داده می‌شود

###### **مدیریت آلارم خطا در چند نقشه به‌صورت هم‌زمان**

در صورتی که خطا به‌صورت هم‌زمان در **چند نقشه‌ی مختلف** رخ دهد، نرم‌افزار رفتار زیر را اعمال می‌کند:

·         برای هر نقشه‌ای که حداقل یک آلارم خطا در آن ثبت شده باشد، یک **تب مجزا** ایجاد می‌شود.

·         عنوان هر تب، **نام نقشه** مطابق با نام تعریف‌شده در تنظیمات سیستم خواهد بود.

·         تمامی تب‌ها در بالای بخش نمایش نقشه قرار می‌گیرند و کاربر می‌تواند به‌سادگی بین آن‌ها جابجا شود.

·         به‌صورت پیش‌فرض، **نقشه‌ای که اولین آلارم خطا در آن شناسایی شده است** (بر اساس ترتیب دریافت رویداد) فعال و نمایش داده می‌شود.

**با انتخاب هر تب توسط کاربر:**

·         تنها نقشه مربوط به همان تب نمایش داده می‌شود.

·         نواحی دچار خطا در آن نقشه به‌صورت **زرد چشمک‌زن** مشخص می‌شوند.

·         لیست خطاهای مرتبط با همان نقشه در پایین نقشه بارگذاری می‌گردد.

·         دکمه‌های کنترلی تنها بر روی رویدادهای همان نقشه اعمال می‌شوند.

###### **دکمه‌های کنترلی آلارم خطا**

کاربر در بخش مدیریت آلارم خطا به گزینه‌های کنترلی زیر دسترسی دارد:

·         **قطع صدای بازر**  
 با انتخاب این گزینه:

o        صدای بازر پنل قطع می‌شود.

o        نمایش زرد چشمک‌زن از روی نقشه حذف می‌گردد.

o        رویداد خطا به حالت مشاهده‌شده منتقل می‌شود.

·         **کنار گذاشتن**  
 با انتخاب این گزینه:

o        پنجره هشدار بسته می‌شود.

o        صدای بازر پنل همچنان فعال باقی می‌ماند.

o        رویداد خطا در وضعیت فعال حفظ می‌شود.

###### **نکات مهم در مدیریت رویدادها و آلارم‌ها**

1\.  	در **رویدادهای حریق**، ابتدا باید صدای آژیر قطع شود و تنها پس از آن امکان قطع صدای بازر پنل فعال خواهد شد.

2\.  	کلیه اقدامات کاربران شامل قطع آژیر، قطع بازر و سایر عملیات کنترلی:

o        به‌همراه **نام کاربری**

o        **تاریخ و زمان دقیق**

در تاریخچه رویدادهای نرم‌افزار ثبت می‌شود و **قابل حذف یا ویرایش نیست**.

3\.  	در صورت انتخاب گزینه **«کنار گذاشتن»**، با هر بار ورود مجدد به نرم‌افزار، هشدار مربوطه مجدداً نمایش داده می‌شود تا زمانی که صدای بازر پنل قطع گردد.

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

 

### **۶.۹.4 مدیریت گزارش‌ها  (Report Management)**

بخش **مدیریت گزارش‌ها** به‌منظور **بررسی، مستندسازی، تحلیل و چاپ وقایع گذشته‌ی پنل اعلام حریق** طراحی شده است. این بخش به‌صورت **کاملاً مستقل از نمایش لحظه‌ای رویدادها (Real-Time Events)** عمل می‌کند و امکان گزارش‌گیری بر اساس بازه‌ی زمانی مشخص را فراهم می‌سازد.

#### **انتخاب بازه‌ی زمانی گزارش**

در بالای صفحه و در بخش مرکزی رابط کاربری، دو فیلد زیر قرار دارد:

·         **تاریخ شروع**

·         **تاریخ پایان**

با کلیک روی هر یک از تکست‌باکس‌ها، **تقویم انتخاب تاریخ** به‌صورت **پاپ‌آپ در زیر همان فیلد** نمایش داده می‌شود. کاربر می‌تواند به‌صورت دقیق:

·         سال

·         ماه

·         روز

را انتخاب نماید.

پس از تعیین بازه‌ی زمانی و انتخاب دکمه **«گزارش‌گیری»**، سیستم فرآیند تحلیل و نمایش گزارش‌ها را آغاز می‌کند.

#### **نمایش گرافیکی رویدادها روی نقشه**

پس از گزارش‌گیری، در پایین بخش انتخاب تاریخ، **نقشه‌ی دو‌بعدی یا سه‌بعدی ساختمان** نمایش داده می‌شود. در این نقشه:

·         **رویدادهای اعلام حریق** با **رنگ قرمز**

·         **رویدادهای خطا** با **رنگ زرد**

بر روی ناحیه یا دیوایسی که دچار رویداد شده است، نمایش داده می‌شوند.

##### **نمایش اطلاعات با حرکت موس (Tooltip)**

با قرار گرفتن نشانگر موس روی هر ناحیه یا دیوایس دارای رویداد، اطلاعات زیر به‌صورت Tooltip نمایش داده می‌شود:

·         نوع رویداد (حریق / خطا)

·         شماره دیوایس

·         تاریخ و زمان وقوع رویداد

·         آدرس دقیق دیوایس

#### **مدیریت چند نقشه در یک بازه‌ی زمانی**

در صورتی که در بازه‌ی زمانی انتخاب‌شده، رویدادها در **چند نقشه‌ی مختلف** ثبت شده باشند، سیستم به‌صورت خودکار فرآیند بررسی رویدادها را به ترتیب زیر انجام می‌دهد:

1\.  	بررسی از **کارت لوپ شماره ۱** آغاز می‌شود.

2\.  	دیوایس‌ها به‌ترتیب از **شماره ۱ تا ۲۵۴** تحلیل می‌شوند.

3\.  	سپس سایر کارت‌های لوپ نیز با همین منطق بررسی می‌گردند.

##### **نحوه نمایش نقشه‌ها به‌صورت تب (Tab)**

در صورت وجود رویداد در چند نقشه:

·         برای **هر نقشه‌ای که حداقل یک رویداد در آن ثبت شده باشد**، یک **تب مجزا** ایجاد می‌شود.

·         عنوان هر تب، **نام نقشه** مطابق با نام تعریف‌شده در تنظیمات سیستم خواهد بود (مانند: همکف، طبقه اول، پارکینگ و …).

·         تمامی تب‌ها در بالای بخش نمایش نقشه قرار می‌گیرند و کاربر می‌تواند به‌سادگی بین آن‌ها جابجا شود.

##### **رفتار پیش‌فرض سیستم**

·         به‌صورت پیش‌فرض، **نقشه‌ای که اولین رویداد در آن شناسایی شده است** (بر اساس ترتیب بررسی لوپ و دیوایس) فعال و نمایش داده می‌شود.

·         با انتخاب هر تب توسط کاربر:

o        تنها **نقشه مربوط به همان تب** نمایش داده می‌شود.

o        اطلاعات مرتبط شامل نمایش گرافیکی رویدادها روی نقشه و جداول اطلاعات مربوطه بارگذاری می‌گردد.

#### **قابلیت‌های جابجایی و بزرگ‌نمایی نقشه**

کاربر می‌تواند با ابزارهای مختلف، به‌راحتی در نقشه حرکت و زوم انجام دهد:

·         **جابجایی نقشه**:

o        نگه داشتن کلید **Space**

o        کلیک چپ موس و کشیدن نقشه

·         **اسکرول خودکار**:

o        در صورتی که ابعاد نقشه بزرگ‌تر از صفحه نمایش باشد، اسکرول‌بارهای **عمودی و افقی** فعال می‌شوند.

·         **ابزار دست (Pan Tool)**:

o        در بالای سمت چپ نقشه، آیکنی به‌شکل **دست** وجود دارد.

o        با فعال‌سازی آن، نشانگر موس تغییر کرده و امکان حرکت آزادانه در نقشه فراهم می‌شود.

·         **بزرگ‌نمایی و کوچک‌نمایی (Zoom)**:

o        با اسکرول موس

o        با دکمه‌های **\+** و **–** در بالای سمت چپ نقشه

o        از طریق اسکرول زوم در پایین سمت چپ صفحه که درصد بزرگ‌نمایی به‌صورت عددی نمایش داده می‌شود

### **جستجو و فیلتر رویدادها**

·         در **بالای سمت راست نقشه**، یک بخش **جستجو (Search)** در نظر گرفته شده است.

·         امکان **فیلتر گزارش‌ها** بر اساس نوع رویداد وجود دارد:

o        فقط حریق

o        فقط خطا

در صورت فعال‌سازی هر فیلتر:

·         تنها همان نوع رویداد بر روی نقشه نمایش داده می‌شود.

#### **جداول اطلاعاتی زیر هر نقشه**

در پایین هر نقشه، **دو جدول مجزا** نمایش داده می‌شود:

##### **جدول اول: جزئیات رویدادهای سیستم**

(عنوان جدول در بالای آن درج می‌شود)

این جدول شامل اطلاعات دقیق فنی هر رویداد است:

·         شماره دیوایس

·         نوع دیوایس

·         نام دیوایس

·         نوع رویداد (حریق / خطا)

·         تاریخ و زمان رویداد

·         آدرس دقیق دیوایس

·         توضیحات

##### **جدول دوم: گزارش فعالیت کاربران سیستم**

*(User Activity Log / Audit Log)*

این جدول، فعالیت کاربران در بازه‌ی زمانی انتخاب‌شده را نمایش می‌دهد و شامل موارد زیر است:

·         ردیف

·         نام کاربر

·         تاریخ و زمان ورود

·         شرح تغییرات انجام‌شده، از جمله:

o        فعال یا غیرفعال کردن آژیر

o        غیرفعال کردن بازر پنل

o        افزودن یا ویرایش دیوایس‌ها (تنظیمات مدیریتی)

o        اضافه یا حذف نقشه‌های دو‌بعدی یا سه‌بعدی

o        جانمایی دیوایس‌ها روی نقشه

o        تغییر رمز عبور

o        و سایر عملیات مدیریتی

#### **مدیریت حجم اطلاعات در جداول**

در صورتی که تعداد رکوردهای هر جدول زیاد باشد:

·         **اسکرول عمودی داخلی** برای جدول فعال می‌شود

·         از شلوغ شدن صفحه جلوگیری شده و کاربر می‌تواند تمام اطلاعات را مرور کند

### **چاپ و خروجی گزارش‌ها**

·         در پایین هر جدول، دکمه‌ای با عنوان **«چاپ»** قرار دارد.

·         با استفاده از این دکمه:

o        گزارش مربوط به **همان نقشه** به‌صورت **PDF** یا **Excel**  ذخیره یا چاپ می‌شود.

o        خروجی شامل هر دو جدول (رویدادها و فعالیت کاربران) خواهد بود.

#### **چاپ کلی گزارش‌ها**

در صورتی که در بازه‌ی زمانی انتخاب‌شده، **چندین نقشه دارای رویداد باشند**:

·         در بالای صفحه و بالای تمامی نقشه‌ها، دکمه‌ای با عنوان **«چاپ همه رویدادها»** نمایش داده می‌شود.

·         با انتخاب این گزینه، **گزارش کامل پنل** شامل تمام نقشه‌ها و تمامی رویدادها در قالب **PDF** یا **Excel** تولید و چاپ می‌گردد.

#### **اطلاعات درج‌شده در سربرگ گزارش چاپی**

در تمامی گزارش‌های چاپ‌شده، اطلاعات زیر درج می‌شود:

·         تاریخ و زمان چاپ گزارش

·         بازه‌ی زمانی انتخاب‌شده برای گزارش‌گیری

·         نام کاربری فرد گزارش‌گیرنده

 

 

 

 

 

 

 

### **6.9.5 کنترل و فرمان‌دهی از راه دور (Remote Control & Command)**

بخش **کنترل و فرمان‌دهی از راه دور** در منوی **مانیتورینگ (Monitoring)** نرم‌افزار قرار دارد و امکان مدیریت برخی عملکردهای حیاتی پنل اعلام حریق را به‌صورت نرم‌افزاری و از راه دور فراهم می‌کند.

این بخش شامل دو زیرمنو است:

·         **قطع صدای هشدار پنل (بازر)**

·         **فعال‌سازی و غیرفعال‌سازی آژیرها**

با انتخاب هر یک از این گزینه‌ها، کاربر به **صفحه‌ای مستقل و اختصاصی** هدایت می‌شود

#### **۶.۹.۵.۱ قطع صدای هشدار پنل (بازر)**

**(Panel Buzzer Silence Control)**

در این بخش، کاربر می‌تواند **صدای هشدار بازر پنل** را به‌صورت دستی قطع نماید.  
 این عملیات تنها با **تأیید کاربر** انجام شده و **به‌صورت کامل در تاریخچه (Log) نرم‌افزار ثبت** می‌شود.

##### **نمایش وضعیت فعال بودن بازر پنل**

در صورتی که صدای هشدار پنل (بازر) فعال باشد:

·         در **بالای نرم‌افزار و صفحه اصلی**، پیامی با **هایلایت زردرنگ** نمایش داده می‌شود با متن:

**«صدای هشدار پنل (بازر) فعال است»**

·         این هشدار به‌صورت **سراسری** در تمامی صفحات نرم‌افزار قابل مشاهده است.

·         در صورت قطع صدای بازر (چه از طریق نرم‌افزار و چه مستقیماً از طریق پنل)، این هایلایت به‌صورت خودکار حذف می‌شود.

##### **روش‌های دسترسی به بخش قطع صدای بازر**

کاربر از دو مسیر می‌تواند به این بخش دسترسی داشته باشد:

1\.  	**منوی مانیتورینگ**

2\.  	**کلیک مستقیم روی هایلایت زردرنگ هشدار فعال بودن بازر**

در هر دو حالت، کاربر به صفحه **قطع صدای هشدار پنل (بازر)** هدایت می‌شود.

##### **نمایش نقشه و وضعیت خطاها**

پس از ورود به این صفحه، در صورتی که بازر پنل فعال باشد:

·         **نقشه دو‌بعدی یا سه‌بعدی ساختمان** نمایش داده می‌شود.

·         نواحی یا دیوایس‌هایی که دچار **خطا** شده‌اند، با **رنگ زرد** مشخص می‌شوند.

##### **قابلیت‌های جابجایی و بزرگ‌نمایی نقشه**

نقشه نمایش‌داده‌شده دارای امکانات کامل جهت مشاهده و بررسی دقیق است:

###### **جابجایی نقشه**

·         نگه داشتن کلید **Space**

·         کلیک چپ موس و کشیدن نقشه

###### **اسکرول خودکار**

·         در صورتی که ابعاد نقشه بزرگ‌تر از صفحه نمایش باشد، اسکرول‌بارهای **عمودی و افقی** فعال می‌شوند.

###### **ابزار دست (Pan Tool)**

·         در بالای سمت چپ نقشه، آیکنی به‌شکل **دست** وجود دارد.

·         با فعال‌سازی این ابزار، نشانگر موس تغییر کرده و امکان حرکت آزادانه در نقشه فراهم می‌شود.

###### **بزرگ‌نمایی و کوچک‌نمایی (Zoom)**

·         استفاده از اسکرول موس

·         دکمه‌های **\+** و **–** در بالای سمت چپ نقشه

·         اسکرول زوم در پایین سمت چپ صفحه که میزان بزرگ‌نمایی به‌صورت **درصد عددی** نمایش داده می‌شود

##### **نمایش اطلاعات خطا با حرکت موس (Tooltip)**

با قرار گرفتن نشانگر موس روی هر ناحیه یا دیوایس دارای خطا، اطلاعات زیر به‌صورت Tooltip نمایش داده می‌شود:

·         شماره دیوایس

·         تاریخ و زمان وقوع خطا

·         آدرس دقیق دیوایس

·         دلایل خطا

##### **جدول اطلاعات خطاها**

اطلاعات مربوط به خطاها به‌صورت کامل و ساخت‌یافته در **جدولی در زیر نقشه** نیز نمایش داده می‌شود تا کاربر بتواند جزئیات را به‌صورت متنی بررسی کند.

##### **نحوه قطع صدای بازر پنل**

کاربر می‌تواند صدای بازر پنل را از دو روش قطع نماید:

1\.  	**از طریق نقشه**

o        با کلیک روی ناحیه یا دیوایس زردرنگ (دارای خطا)،

o        دکمه‌ای با عنوان **«قطع صدای هشدار پنل (بازر)»** نمایش داده می‌شود.

o        با انتخاب این دکمه، صدای بازر پنل قطع خواهد شد.

2\.  	**از طریق دکمه کنترلی کنار جدول**

o        در پایین و کنار جدول اطلاعات، دکمه‌ای برای **قطع صدای بازر** قرار دارد.

o        با کلیک روی این دکمه نیز عملیات قطع هشدار انجام می‌شود.

پس از قطع صدای هشدار پنل (بازر)، هایلایت زردرنگ مربوط به خطاها از روی نقشه حذف شده و لیست اطلاعات نمایش‌داده‌شده به‌صورت خودکار خالی می‌شود.

در صورتی که **چندین دیوایس به‌صورت هم‌زمان دچار خطا شده باشند** و این دیوایس‌ها در **نقشه‌های متفاوت** (مانند طبقات یا بخش‌های مختلف ساختمان) قرار داشته باشند، سیستم تمامی نقشه‌های مرتبط را به‌صورت **تفکیک‌شده** نمایش می‌دهد.

این تفکیک می‌تواند بر اساس **نام نقشه** و در قالب **تب (Tab)** انجام شود، به‌گونه‌ای که:

·         برای هر نقشه‌ای که حداقل یک خطا در آن ثبت شده است، یک **تب مجزا** با نام همان نقشه (مثلاً همکف، طبقه اول، پارکینگ و …) ایجاد می‌شود.

·         کاربر با انتخاب هر تب، می‌تواند **نقشه مربوط به همان بخش** را مشاهده کرده و وارد **جزئیات خطاها** شامل نمایش گرافیکی روی نقشه، Tooltipها و جدول اطلاعات شود.

·         در صورتی که چندین نقشه دارای خطا باشند، **تمامی تب‌ها به‌صورت هم‌زمان در دسترس** خواهند بود.

·         به‌صورت پیش‌فرض، **نقشه‌ای که اولین خطا در آن ثبت شده است** در هنگام ورود به صفحه فعال و نمایش داده می‌شود تا کاربر سریع‌تر به مهم‌ترین رویداد دسترسی داشته باشد.

این ساختار تب‌بندی باعث می‌شود:

·         از شلوغی رابط کاربری جلوگیری شود،

·         جابجایی بین نقشه‌ها ساده‌تر انجام گیرد،

·         و کاربر بتواند خطاها را به‌صورت **سریع، هدفمند و طبقه‌بندی‌شده** بررسی و مدیریت کند.

### **ثبت عملیات در تاریخچه سیستم**

·         هر بار قطع صدای بازر:

o        به‌صورت دقیق در **گزارش‌ها و لاگ نرم‌افزار** ثبت می‌شود.

o        شامل نام کاربر، تاریخ و زمان انجام عملیات خواهد بود.

 

 

#### **6.9.5.2 فعال‌سازی و غیرفعال‌سازی آژیرها**

در ابتدا در هنگام ورود کاربر به صفحه ی فعال سازی و غیرفعال سازی آژیرها به 4 روش می توان آژیرها را فعال و غیر فعال کرد که این چهار مورد به صورت تب بندی برای ما نمایش داده می شود که به شرح زیر است:

کاربر می‌تواند آژیرها را به صورت تکی، گروهی یا کلی از طریق روش‌های زیر کنترل کند:

·         **انتخاب از روی نقشه دو‌بعدی یا سه‌بعدی:**  
 با انتخاب آژیر از نقشه، امکان فعال یا غیرفعال کردن آن وجود دارد.  
 اگر آژیر فعال باشد، گزینه «غیرفعال کردن» و بالعکس نمایش داده می‌شود.

·         **انتخاب گروه آژیر:**  
 در صورت گروه‌بندی آژیرها، امکان انتخاب گروه برای فعال یا غیرفعال کردن آن‌ها وجود دارد.

·         **وارد کردن آدرس آژیر:**  
 با وارد کردن شماره یا آدرس آژیر مورد نظر، می‌توان آن را فعال یا غیرفعال کرد.

·         **دکمه‌های فعال/غیرفعال‌سازی کل آژیرها:**  
 امکان فعال یا غیرفعال کردن کلیه آژیرهای سیستم به‌صورت همزمان وجود دارد.

##### **1\. نمایش نقشه دوبعدی و سه‌بعدی در صفحه فعال تب**

هنگام ورود کاربر به این صفحه، تمامی نقشه‌ها به صورت **تب‌بندی شده** نمایش داده می‌شوند. هر تب مطابق با نام نقشه تعریف‌شده در سیستم (مثلاً همکف، طبقه اول، پارکینگ و ...) بوده و کاربر می‌تواند به‌سادگی بین آن‌ها جابجا شود.

* **تب اولین نقشه به‌صورت پیش‌فرض فعال و نمایش داده می‌شود.**

###### **مدیریت نمایش نقشه‌ها و تب‌بندی**

·         تمامی نقشه‌ها به‌صورت تب‌های مجزا در بالای بخش نمایش نقشه‌ها قرار می‌گیرند.

·         عنوان هر تب، نام نقشه مطابق با تنظیمات سیستم است.

·         کاربر می‌تواند با کلیک روی هر تب، نقشه مربوط به آن بخش ساختمان را مشاهده کند.

###### **امکانات کار با نقشه**

نقشه نمایش‌داده‌شده دارای قابلیت‌های زیر است تا کاربر بتواند به‌راحتی آژیرها را مدیریت کند:

·         **جابجایی نقشه (Pan):**

o        نگه داشتن کلید **Space**

o        کلیک چپ موس و کشیدن نقشه

·         **اسکرول خودکار:**

o        در صورتی که ابعاد نقشه بزرگ‌تر از صفحه نمایش باشد، اسکرول‌بارهای **عمودی و افقی** فعال می‌شوند.

·         **ابزار دست (Pan Tool):**

o        در بالای سمت چپ نقشه، آیکونی به‌شکل **دست** قرار دارد.

o        با فعال‌سازی آن، نشانگر موس تغییر کرده و امکان حرکت آزادانه روی نقشه فراهم می‌شود.

·         **بزرگ‌نمایی و کوچک‌نمایی (Zoom):**

o        با استفاده از اسکرول موس

o        با دکمه‌های **\+** و **–** در بالای سمت چپ نقشه

o        با اسکرول زوم در پایین سمت چپ صفحه که میزان بزرگ‌نمایی به‌صورت **درصد عددی** نمایش داده می‌شود

###### **نمایش و انتخاب دیوایس‌ها روی نقشه**

·         تمامی دیوایس‌ها (آژیرها) روی نقشه به صورت دقیق چینش شده‌اند.

·         وقتی موس روی هر دیوایس قرار می‌گیرد، به‌صورت اتوماتیک زیر آن اطلاعات زیر نمایش داده می‌شود و با دور شدن موس، اطلاعات محو می‌شود:

o        شماره لوپ

o        شماره دیوایس

o        نوع دیوایس

o        نام دیوایس

o        آدرس دیوایس

###### **انتخاب و مدیریت آژیرها**

·         با کلیک روی هر آژیر، یک چک‌باکس (تیک) کنار آن ظاهر می‌شود که نشان‌دهنده انتخاب آژیر است.  
·         در صورتی که بار دیگر روی همان چک‌باکس یا دیوایس کلیک شود، تیک آن برداشته شده و از انتخاب خارج می‌شود.  
·         آژیرهای انتخاب‌شده در یک جدول اختصاصی در پایین تب نقشه‌ها نمایش داده می‌شوند.  
·         عنوان این جدول می‌تواند به نام لیست مدیریت آژیرها است و مشخصات جدول به شرح زیر است:  
o        ستون اول جدول شامل چک‌باکس‌هایی است که امکان انتخاب یا لغو انتخاب تک‌تک آژیرها را فراهم می‌کند.  
o        همچنین گزینه‌ای برای **انتخاب همه آژیرها** نیز وجود دارد.  
o        در زیر جدول، دکمه‌های کنترلی قرار دارند:  
§         **فعال‌سازی آژیرها:** زمانی که آژیر غیرفعال است  
§         **غیرفعال‌سازی آژیرها:** زمانی که آژیر فعال است  
§         **لغو انتخاب‌ها:**  با فشردن این دکمه، آژیرهای انتخاب‌شده از جدول و از انتخاب نقشه‌ها حذف می‌شوند.

### **پاپ‌آپ تأیید برای دکمه‌ها:**

با کلیک روی هر یک از دکمه‌های **فعال‌سازی**، **غیرفعال‌سازی** یا **لغو انتخاب‌ها**، یک پنجره پاپ‌آپ نمایش داده می‌شود که متن آن به شکل زیر است:

«آیا مطمئن هستید که می‌خواهید این عملیات را انجام دهید؟»

و شامل دو گزینه می‌باشد:

·         **بله**: عملیات مورد نظر انجام می‌شود.

·         **خیر**: عملیات لغو شده و هیچ تغییری ایجاد نمی‌شود.

 

###### **قابلیت انتخاب آژیرها در چند نقشه**

·         کاربر می‌تواند به صورت همزمان آژیرهایی را از نقشه‌های مختلف انتخاب کند.

·         مثلاً یک آژیر از نقشه همکف و یک آژیر از نقشه طبقه چهارم را انتخاب کرده و در لیست جدول، به صورت یکپارچه مشاهده و مدیریت کند.

·         سپس می‌تواند همه آژیرهای انتخابی در نقشه های متفاوت را به‌صورت گروهی فعال یا غیرفعال کند.

###### **تغییر وضعیت آژیرها**

·         پس از فعال‌سازی آژیرها، دکمه مربوط به فعال‌سازی به **غیرفعال‌سازی** تغییر می‌کند و بالعکس.

·         لیست آژیرهای فعال در جدول نمایش داده می‌شود و کاربر می‌تواند تک‌تک یا کلی آن‌ها را از طریق چک‌باکس‌ها مدیریت کند.

###### **پیام هشدار در صورت عدم انتخاب آژیر**

·         اگر کاربر بدون انتخاب چک‌باکس‌ها، بخواهد عملیات فعال، غیرفعال یا حذف را انجام دهد، یک **پنجره هشدار (پاپ‌آپ)** نمایش داده می‌شود با پیام:  
 **«برای انجام عملیات، لطفاً حداقل یک آژیر را انتخاب کنید»**

·         کاربر می‌تواند با کلیک روی علامت ضربدر پاپ‌آپ را ببندد و به کار خود ادامه دهد.

##### **2\. انتخاب گروه آژیرها**

·         هنگام کلیک روی این تب، صفحه‌ای باز می‌شود که در آن **گروه‌های آژیر** (در صورت وجود گروه‌بندی قبلی) به صورت **چک‌باکس** نمایش داده می‌شوند.

·         کاربر می‌تواند یک یا چند گروه را همزمان انتخاب کند.

o        با انتخاب هر گروه، در بخش پایین و سمت چپ صفحه، نام گروه نمایش داده می‌شود.

o        زیر نام گروه، جدولی شامل تمامی آژیرهای آن گروه با مشخصات زیر لیست می‌شود:

§         شماره لوپ

§         شماره دیوایس

§         نوع دیوایس

§         نام دیوایس

§         آدرس دیوایس

·         هر دیوایس در یک ردیف جداگانه قرار دارد و در ابتدای هر ردیف، یک چک‌باکس با عنوان **انتخاب** وجود دارد که به‌صورت پیش‌فرض فعال است.

o        کاربر می‌تواند هر کدام از آژیرها را به‌دلخواه از انتخاب خارج کند (تیک چک‌باکس را بردارد) تا آن‌ها را در عملیات فعال/غیرفعال‌سازی لحاظ نکند.

·         برای انتخاب چند گروه به صورت همزمان، کافی است چک‌باکس‌های کنار نام هر گروه را فعال کنید.

o        آژیرهای هر گروه به ترتیب در جداول مربوط به همان گروه نمایش داده می‌شوند.

·         پس از انتخاب گروه یا گروه‌ها، در بالای صفحه دکمه‌هایی نمایش داده می‌شوند:

o        **فعال‌سازی** (در صورتی که آژیرها غیرفعال باشند)

o        **غیرفعال‌سازی** (اگر آژیرها فعال باشند)

o        **لغو گروه** (با فشردن این دکمه، تمامی گروه‌های انتخاب‌شده لغو می‌شوند.)

·         اگر نمی‌خواهید همه گروه‌های انتخاب‌شده لغو شود، می‌توانید به صورت جداگانه تیک چک‌باکس کنار هر نام گروه را بردارید تا فقط همان گروه از انتخاب خارج شود.

###### **پاپ‌آپ تأیید برای دکمه‌ها:**

با کلیک روی هر یک از دکمه‌های فعال‌سازی یا غیرفعال‌سازی، یک پنجره پاپ‌آپ باز می‌شود که متن آن به شکل زیر است:

«آیا مطمئن هستید که می‌خواهید این عملیات را انجام دهید؟»

و شامل دو گزینه است:

·         **بله**: عملیات فعال‌سازی یا غیرفعال‌سازی گروه(های) انتخاب‌شده انجام می‌شود.

·         **خیر**: عملیات لغو شده و هیچ تغییری صورت نمی‌گیرد.

 

##### **3\. وارد کردن آدرس آژیر**

با انتخاب این تب، عنوانی در بالای صفحه نمایش داده می‌شود با متن:  
 **«لطفاً آدرس آژیر را در تکس‌باکس وارد کنید»**  
 و در کنار آن یک تکس‌باکس جهت وارد کردن آدرس آژیر وجود دارد.

کاربر می‌تواند آدرس هر آژیر را به‌صورت تک‌تک در این تکس‌باکس وارد کرده و پس از زدن دکمه **انتخاب**، مشخصات آن آژیر در لیست زیر تکس‌باکس نمایش داده می‌شود.  
 مشخصات نمایش داده‌شده شامل:

* شماره لوپ  
* شماره دیوایس  
* نوع دیوایس  
* نام دیوایس  
* آدرس دیوایس

برای انتخاب چند آژیر، کافی است آدرس آژیرهای بعدی را نیز یکی‌یکی وارد کرده و دکمه **انتخاب** را بزنید تا تمام آژیرهای وارد شده در لیست جدول زیر نمایش داده شوند.

در انتهای این جدول، دکمه‌های کنترلی زیر قرار دارند:

* **فعال‌سازی / غیرفعال‌سازی آژیرها**  
   (عنوان دکمه با توجه به وضعیت فعلی آژیرها به‌صورت پویا تغییر می‌کند)

با انتخاب این دکمه، یک **پنجره پاپ‌آپ تأیید** نمایش داده می‌شود با پیام:  
 **«آیا مطمئن هستید که می‌خواهید این عملیات را انجام دهید؟»**

این پاپ‌آپ شامل دو گزینه است:

* **بله:** عملیات فعال‌سازی یا غیرفعال‌سازی آژیرهای انتخاب‌شده انجام می‌شود.  
  * **خیر:** عملیات لغو شده و هیچ تغییری اعمال نمی‌گردد.  
* **لغو انتخاب‌ها**  
   با فشردن این دکمه، تمامی آژیرهای انتخاب‌شده از لیست حذف شده و جدول خالی می‌شود.

**قابلیت نمایش درختی (Tree View) در سمت راست صفحه:**

* در این بخش، آژیرها به صورت سلسله‌مراتبی نمایش داده می‌شوند.  
* ابتدا شماره لوپ‌ها نمایش داده می‌شود و با کلیک روی هر لوپ، آژیرهای مربوط به آن لوپ همراه با شماره دیوایس و آدرس‌شان باز و نمایش داده می‌شوند.  
* کاربر می‌تواند با کلیک روی هر ردیف (لوپ یا دیوایس) آن را انتخاب کرده و به لیست اصلی اضافه کند.  
* همچنین با کلیک روی نام لوپ، تمام آژیرهای آن لوپ به صورت یکجا در لیست انتخاب‌شده‌ها قرار می‌گیرند.

##### **4\. دکمه‌های فعال/غیرفعال‌سازی کل آژیرها**

با انتخاب این بخش، صفحه‌ای نمایش داده می‌شود که شامل یک دکمه با عنوان **فعال‌سازی یا غیرفعال‌سازی آژیر** است.

* اگر آژیر در حال حاضر **فعال** باشد، دکمه به صورت **غیرفعال‌سازی** نمایش داده می‌شود.  
* اگر آژیر **غیرفعال** باشد، دکمه به صورت **فعال‌سازی** ظاهر می‌شود.

با کلیک روی این دکمه، یک پنجره پاپ‌آپ باز می‌شود که متن آن بسته به نوع عملیات به صورت زیر است:

«آیا مطمئن هستید که می‌خواهید این عملیات را انجام دهید؟»

و دو دکمه دارد:

* **بله**: در صورت انتخاب این گزینه، عملیات فعال‌سازی یا غیرفعال‌سازی آژیر انجام می‌شود.  
* **خیر**: در صورت انتخاب این گزینه، عملیات لغو شده و هیچ تغییری صورت نمی‌گیرد.

**نکات مهم:**

* این تاییدیه به منظور جلوگیری از انجام عملیات ناخواسته و خطاهای کاربری در نظر گرفته شده است.  
* تمامی عملیات فعال‌سازی یا غیرفعال‌سازی همراه با نام کاربری و زمان دقیق در تاریخچه نرم‌افزار ثبت می‌گردد و امکان ویرایش یا حذف آن وجود ندارد.

## **نکات مهم در ثبت عملیات**

·         تمامی اقدامات کاربران مانند فعال‌سازی یا غیرفعال‌سازی آژیرها (تک‌تک، گروهی یا کلی)

o        همراه با **نام کاربری**

o        و **زمان دقیق عملیات**  
 در تاریخچه نرم‌افزار ثبت می‌شود.

·         این تاریخچه قابل **چاپ** بوده و امکان **حذف یا ویرایش آن وجود ندارد**.

### **6.9.6 تست و عیب‌یابی تجهیزات**

امکان تست و بررسی وضعیت عملکرد تمامی تجهیزات سیستم اعلام حریق شامل دتکتورها، آژیرها و سایر دیوایس‌ها از طریق نرم‌افزار فراهم شده است. برای استفاده از این قابلیت، ابتدا باید نقشه دوبعدی یا سه‌بعدی ساختمان در بخش مربوط به نقشه‌ها آپلود شده و کلیه دیوایس‌ها به‌صورت دقیق در نقشه چیده و آدرس‌دهی شده باشند.

#### **نحوه نمایش و انتخاب نقشه‌ها**

·         با ورود به این منو، تمامی نقشه‌ها به صورت تب‌بندی شده (Tab) در پایین صفحه نمایش داده می‌شوند.

·         عنوان هر تب، نام نقشه مطابق با تنظیمات سیستم است.

·         اولین تب (نقشه) به صورت پیش‌فرض به کاربر نمایش داده می‌شود و قابل مشاهده است.

#### **مشاهده اطلاعات دیوایس‌ها**

·         با قرار گرفتن نشانگر موس روی هر دیوایس، اطلاعات زیر به‌صورت اتوماتیک در زیر نقشه نمایش داده می‌شود:

o        شماره لوپ

o        شماره دیوایس

o        نوع دیوایس

o        آدرس دیوایس

o        وضعیت دیوایس (فعال یا غیرفعال)

#### **فعال‌سازی و غیرفعال‌سازی دیوایس‌ها**

·         کاربر می‌تواند با کلیک روی هر دیوایس، وضعیت آن را تغییر دهد:

o        اگر دیوایس فعال نباشد، با کلیک روی آن، دیوایس فعال شده و به صورت **نور آبی چشمک‌زن (Blinking Blue Light)** نمایش داده می‌شود.

§         روشن بودن این نور نشان‌دهنده این است که دیوایس با موفقیت فعال شده است.

o        اگر دیوایس فعال باشد، با کلیک مجدد روی آن، دیوایس غیرفعال شده و نور آبی چشمک‌زن خاموش می‌شود.

·         این عملکرد زمانی برقرار است که دیوایس هیچ مشکل فنی نداشته باشد.

·         در صورت وجود مشکل یا خطا در دیوایس، نرم‌افزار به‌صورت پاپ‌آپ پیغام می‌دهد که:

«دیوایس \[شماره یا نام دیوایس\] دارای خطا است و امکان فعال یا غیرفعال کردن وجود ندارد»

#### **نکات مهم**

·         امکان فعال‌سازی یا غیرفعال‌سازی دیوایس‌ها فقط در صورتی وجود دارد که دیوایس سالم و بدون مشکل باشد.

·         تمامی عملیات انجام شده توسط کاربر، شامل تغییر وضعیت دیوایس‌ها، به همراه نام کاربری، تاریخ و ساعت دقیق در بخش گزارش‌ها (History) ثبت می‌شود و امکان مشاهده یا چاپ آن فراهم است.

·         این گزارش‌ها قابل حذف یا ویرایش نیستند تا امنیت و صحت اطلاعات حفظ شود.

 

## **ثبت فعالیت کاربران (Audit Log)**

سیستم نرم‌افزاری به‌صورت کامل و دقیق تمامی عملیات انجام شده توسط کاربران را ثبت می‌کند تا امکان پیگیری و بررسی فعالیت‌ها فراهم باشد. این ثبت شامل تمامی اقدامات مهم از جمله فعال‌سازی یا غیرفعال‌سازی آژیرها، قطع صدای بازر پنل، انجام تست تجهیزات و سایر عملیات کنترلی می‌باشد.

هر رکورد ثبت‌شده حاوی اطلاعات زیر است:

·         نام کاربری فردی که عملیات را انجام داده است

·         تاریخ دقیق انجام عملیات

·         ساعت دقیق انجام عملیات

این اطلاعات در بخش گزارش‌ها (History) قابل مشاهده و بررسی است و کاربر مجاز می‌تواند گزارش‌ها را مشاهده، چاپ و برای مستندسازی ذخیره کند.

برای حفظ امنیت و صحت عملکرد سیستم، داده‌های ثبت شده در لاگ فعالیت‌ها **قابل حذف یا ویرایش نیستند** و این امر تضمین می‌کند که سابقه کاملی از تمامی فعالیت‌ها به صورت شفاف و دقیق حفظ شود.

 

